/* parser generated by jison 0.4.13 */
/*
  Returns a Parser object of the following structure:

  Parser: {
    yy: {}
  }

  Parser.prototype: {
    yy: {},
    trace: function(),
    symbols_: {associative list: name ==> number},
    terminals_: {associative list: number ==> name},
    productions_: [...],
    performAction: function anonymous(yytext, yyleng, yylineno, yy, yystate, $$, _$),
    table: [...],
    defaultActions: {...},
    parseError: function(str, hash),
    parse: function(input),

    lexer: {
        EOF: 1,
        parseError: function(str, hash),
        setInput: function(input),
        input: function(),
        unput: function(str),
        more: function(),
        less: function(n),
        pastInput: function(),
        upcomingInput: function(),
        showPosition: function(),
        test_match: function(regex_match_array, rule_index),
        next: function(),
        lex: function(),
        begin: function(condition),
        popState: function(),
        _currentRules: function(),
        topState: function(),
        pushState: function(condition),

        options: {
            ranges: boolean           (optional: true ==> token location info will include a .range[] member)
            flex: boolean             (optional: true ==> flex-like lexing behaviour where the rules are tested exhaustively to find the longest match)
            backtrack_lexer: boolean  (optional: true ==> lexer regexes are tested in order and for each matching regex the action code is invoked; the lexer terminates the scan when a token is returned by the action code)
        },

        performAction: function(yy, yy_, $avoiding_name_collisions, YY_START),
        rules: [...],
        conditions: {associative list: name ==> set},
    }
  }


  token location info (@$, _$, etc.): {
    first_line: n,
    last_line: n,
    first_column: n,
    last_column: n,
    range: [start_number, end_number]       (where the numbers are indexes into the input string, regular zero-based)
  }


  the parseError function receives a 'hash' object with these members for lexer and parser errors: {
    text:        (matched text)
    token:       (the produced terminal token, if any)
    line:        (yylineno)
  }
  while parser (grammar) errors will also provide these members, i.e. parser errors deliver a superset of attributes: {
    loc:         (yylloc)
    expected:    (string describing the set of expected tokens)
    recoverable: (boolean: TRUE when the parser has a error recovery rule available for this particular error)
  }
*/
var manana_parser = (function(){
var parser = {trace: function trace() { },
yy: {},
symbols_: {"error":2,"manana":3,"ast":4,"EOF":5,"stmt":6,"block":7,"INDENT":8,"stmts":9,"DEDENT":10,"tag":11,"void":12,"RAW":13,"text":14,"condition":15,"for":16,"fn":17,"alias":18,"unalias":19,"include":20,"switch":21,"with":22,"code":23,"FILTER":24,"BREAK":25,"CONTINUE":26,"TAG":27,"shorthandattrs":28,"attrlist":29,"VOIDTAG":30,"shorthandattr":31,"TAGID":32,"TAGCLASS":33,"TAGSRC":34,"ATTR":35,"=":36,"string":37,"DATAATTR":38,"(":39,")":40,"void_tag_stmt":41,"attrs":42,"CODETAG":43,"src":44,"srcline":45,"LINE":46,"BLANKLINE":47,"branches":48,"if":49,"else":50,"elifs":51,"IF":52,"exprs":53,"elif":54,"ELIF":55,"ELSE":56,"expr":57,"AND":58,"OR":59,"EXISTS":60,"ev":61,"NOT":62,"OP":63,"IN":64,"IS":65,"TYPE":66,"path":67,"INT":68,"bool":69,"ALIAS":70,"path_or_fn":71,"AS":72,"ID":73,"UNALIAS":74,"INCLUDE":75,"FOR":76,"CASE":77,"cases":78,"case":79,"WHEN":80,"WITH":81,"TEXT":82,"name":83,"STR":84,"ISTR":85,"NSTART":86,"NSTOP":87,"COMMA":88,"default":89,"id":90,".":91,"[":92,"]":93,":":94,"FN":95,"RPAREN":96,"fnargs":97,"fnarg":98,"hash":99,"EQ":100,"BOOL":101,"$accept":0,"$end":1},
terminals_: {2:"error",5:"EOF",8:"INDENT",10:"DEDENT",12:"void",13:"RAW",24:"FILTER",25:"BREAK",26:"CONTINUE",27:"TAG",30:"VOIDTAG",32:"TAGID",33:"TAGCLASS",34:"TAGSRC",35:"ATTR",36:"=",38:"DATAATTR",39:"(",40:")",42:"attrs",43:"CODETAG",46:"LINE",47:"BLANKLINE",52:"IF",55:"ELIF",56:"ELSE",58:"AND",59:"OR",60:"EXISTS",62:"NOT",63:"OP",64:"IN",65:"IS",66:"TYPE",68:"INT",70:"ALIAS",72:"AS",73:"ID",74:"UNALIAS",75:"INCLUDE",76:"FOR",77:"CASE",80:"WHEN",81:"WITH",82:"TEXT",84:"STR",85:"ISTR",86:"NSTART",87:"NSTOP",88:"COMMA",91:".",92:"[",93:"]",94:":",95:"FN",96:"RPAREN",99:"hash",100:"EQ",101:"BOOL"},
productions_: [0,[3,2],[4,2],[4,1],[4,1],[7,3],[9,2],[9,1],[6,1],[6,1],[6,2],[6,1],[6,1],[6,1],[6,1],[6,1],[6,1],[6,1],[6,1],[6,1],[6,4],[6,1],[6,1],[11,3],[11,3],[11,2],[11,3],[11,3],[11,2],[11,2],[11,2],[11,1],[11,1],[11,2],[11,2],[28,2],[28,1],[31,1],[31,1],[31,1],[29,3],[29,3],[29,4],[29,4],[29,3],[41,1],[41,2],[23,3],[23,4],[44,2],[44,1],[45,2],[45,1],[15,1],[48,1],[48,2],[48,2],[48,3],[49,3],[51,1],[51,2],[54,3],[50,2],[53,1],[53,3],[53,3],[57,2],[57,3],[57,3],[57,1],[57,2],[57,3],[57,4],[57,3],[57,4],[61,1],[61,1],[61,1],[61,1],[61,1],[18,4],[19,2],[20,2],[20,2],[16,5],[16,5],[21,5],[21,6],[78,1],[78,2],[79,3],[22,3],[22,5],[14,2],[14,2],[14,1],[14,1],[37,1],[37,1],[83,3],[83,5],[89,1],[89,1],[89,1],[89,1],[89,1],[67,1],[67,3],[90,1],[90,4],[90,6],[90,6],[90,4],[90,6],[90,6],[90,5],[90,5],[90,4],[17,2],[17,3],[97,3],[97,1],[98,1],[98,1],[98,1],[98,1],[98,1],[98,1],[98,1],[98,3],[98,3],[98,3],[98,3],[98,3],[98,3],[98,3],[71,1],[71,1],[69,1]],
performAction: function anonymous(yytext, yyleng, yylineno, yy, yystate /* action[1] */, $$ /* vstack */, _$ /* lstack */
/**/) {
/* this == yyval */

var $0 = $$.length - 1;
switch (yystate) {
case 1: console.log(JSON.stringify($$[$0-1], null, 4)); return $$[$0-1];
break;
case 2:this.$ = $$[$0-1]; this.$.push($$[$0]);
break;
case 3:this.$ = [$$[$0]];
break;
case 4:this.$ = $$[$0];
break;
case 5:this.$ = $$[$0-1];
break;
case 6:$$[$0-1].push($$[$0]); this.$ = $$[$0-1];
break;
case 7:this.$ = [$$[$0]];
break;
case 10:this.$ = $$[$0];
break;
case 20:this.$ = new FilterNode($$[$0-3], $$[$0-1], new Loc(_$[$0-3], _$[$0-1]));
break;
case 21:this.$ = new BreakNode(new Loc(_$[$0], _$[$0]));
break;
case 22:this.$ = new ContinueNode(new Loc(_$[$0], _$[$0]));
break;
case 23:this.$ = new TagNode($$[$0-2], $$[$0-1]  , $$[$0]  , new Loc(_$[$0-2], _$[$0]));
break;
case 24:this.$ = new TagNode($$[$0-2], $$[$0-1]  , [$$[$0]], new Loc(_$[$0-2], _$[$0]));
break;
case 25:this.$ = new TagNode($$[$0-1], $$[$0]  , []  , new Loc(_$[$0-1], _$[$0]));
break;
case 26:this.$ = new TagNode($$[$0-2], $$[$0-1]  , $$[$0]  , new Loc(_$[$0-2], _$[$0]));
break;
case 27:this.$ = new TagNode($$[$0-2], $$[$0-1]  , [$$[$0]], new Loc(_$[$0-2], _$[$0]));
break;
case 28:this.$ = new TagNode($$[$0-1], $$[$0]  , []  , new Loc(_$[$0-1], _$[$0]));
break;
case 29:this.$ = new TagNode($$[$0-1], null, $$[$0]  , new Loc(_$[$0-1], _$[$0]));
break;
case 30:this.$ = new TagNode($$[$0-1], null, [$$[$0]], new Loc(_$[$0-1], _$[$0]));
break;
case 31:this.$ = new TagNode($$[$0], null, []  , new Loc(_$[$0], _$[$0]));
break;
case 32:this.$ = new VoidTagNode($$[$0], null, new Loc(_$[$0], _$[$0]));
break;
case 33:this.$ = new VoidTagNode($$[$0-1], $$[$0]  , new Loc(_$[$0-1], _$[$0]));
break;
case 34:this.$ = new VoidTagNode($$[$0-1], $$[$0]  , new Loc(_$[$0-1], _$[$0]));
break;
case 35:
      var key = $$[$0][0];
      var val = $$[$0][1];

      this.$ = $$[$0-1];
      if (key === "class" && "class" in this.$) { 
        this.$.class += ' ' + val;
      } else {
        this.$[key] = val;
      }
    
break;
case 36:this.$ = {}; this.$[$$[$0][0]] = $$[$0][1];
break;
case 37:this.$ = ["id", $$[$0]];
break;
case 38:this.$ = ["class", $$[$0]];
break;
case 39:this.$ = ["src", new StringNode($$[$0], new Loc(_$[$0], _$[$0]))];
break;
case 40:this.$ = {}; this.$[$$[$0-2]] = $$[$0];
break;
case 41:this.$ = {}; this.$["data-"+$$[$0-2].slice(1)] = $$[$0];
break;
case 42:this.$ = $$[$0-3]; this.$[$$[$0-2]] = $$[$0];
break;
case 43:this.$ = $$[$0-3]; this.$["data-"+$$[$0-2].slice(1)] = $$[$0];
break;
case 44:this.$ = $$[$0-1];
break;
case 45:this.$ = new VoidTagNode($$[$0], null, new Loc(_$[$0], _$[$0]));
break;
case 46:this.$ = new VoidTagNode($$[$0-1], $$[$0],   new Loc(_$[$0-1], _$[$0]));
break;
case 47:this.$ = new CodeTagNode($$[$0-2], null, $$[$0-1], new Loc(_$[$0-2], _$[$0-1]));
break;
case 48:this.$ = new CodeTagNode($$[$0-3], $$[$0-2],   $$[$0-1], new Loc(_$[$0-3], _$[$0-1]));
break;
case 49:this.$ = $$[$0-1]; this.$.push($$[$0]);
break;
case 50:this.$ = [$$[$0]];
break;
case 51:this.$ = $$[$0-1] + $$[$0] 
break;
case 52:this.$ = '';
break;
case 53:this.$ = new IfNode($$[$0], new Loc(_$[$0], _$[$0]));
break;
case 54:this.$ = [$$[$0]];
break;
case 55:this.$ = [$$[$0-1]]; this.$.push($$[$0]);
break;
case 56:this.$ = [$$[$0-1]]; this.$.push.apply(this.$, $$[$0]);
break;
case 57:this.$ = [$$[$0-2]]; this.$.push.apply(this.$, $$[$0-1]); this.$.push($$[$0]);
break;
case 58:this.$ = new ConditionBranchNode($$[$0-2], $$[$0-1], $$[$0], new Loc(_$[$0-2], _$[$0]));
break;
case 59:this.$ = [$$[$0]];
break;
case 60:this.$ = $$[$0-1]; this.$.push($$[$0]);
break;
case 61:this.$ = new ConditionBranchNode($$[$0-2], $$[$0-1], $$[$0], new Loc(_$[$0-2], _$[$0]));
break;
case 62:this.$ = new ConditionBranchNode($$[$0-1], [], $$[$0], new Loc(_$[$0-1], _$[$0]));
break;
case 63:this.$ = [$$[$0]];
break;
case 64:this.$ = $$[$0-2]; $$[$0].relation = $$[$0-1]; this.$.push($$[$0]);
break;
case 65:this.$ = $$[$0-2]; $$[$0].relation = $$[$0-1]; this.$.push($$[$0]);
break;
case 66:this.$ = new ExpressionNode($$[$0-1]  , $$[$0], null, false  , new Loc(_$[$0-1], _$[$0]));
break;
case 67:this.$ = new ExpressionNode($$[$0-1]  , $$[$0], null, true   , new Loc(_$[$0-2], _$[$0]));
break;
case 68:this.$ = new ExpressionNode($$[$0-1]  , $$[$0-2], $$[$0]  , false  , new Loc(_$[$0-2], _$[$0]));
break;
case 69:this.$ = new ExpressionNode(true, $$[$0], null, false  , new Loc(_$[$0], _$[$0]));
break;
case 70:this.$ = new ExpressionNode(true, $$[$0], null, true   , new Loc(_$[$0-1], _$[$0]));
break;
case 71:this.$ = new ExpressionNode($$[$0-1]  , $$[$0-2], $$[$0]  , false  , new Loc(_$[$0-2], _$[$0]));
break;
case 72:this.$ = new ExpressionNode($$[$0-1]  , $$[$0-3], $$[$0]  , true   , new Loc(_$[$0-3], _$[$0]));
break;
case 73:this.$ = new ExpressionNode($$[$0-1]  , $$[$0-2], $$[$0]  , false  , new Loc(_$[$0-2], _$[$0]));
break;
case 74:this.$ = new ExpressionNode($$[$0-1]  , $$[$0-3], $$[$0]  , true   , new Loc(_$[$0-3], _$[$0]));
break;
case 80:this.$ = new AliasNode($$[$0-2], $$[$0], new Loc(_$[$0-3], _$[$0]));
break;
case 81:this.$ = new UnaliasNode($$[$0], new Loc(_$[$0-1], _$[$0]));
break;
case 82:this.$ = new IncludeNode($$[$0], new Loc(_$[$0-1], _$[$0]));
break;
case 83:this.$ = new IncludeNode($$[$0], new Loc(_$[$0-1], _$[$0]));
break;
case 84:this.$ = new ForNode($$[$0-3], $$[$0-1], $$[$0], new Loc(_$[$0-4], _$[$0]));
break;
case 85:this.$ = new ForNode($$[$0-3], $$[$0-1], $$[$0], new Loc(_$[$0-4], _$[$0]));
break;
case 86:this.$ = new SwitchNode($$[$0-3], $$[$0-1], null   , new Loc(_$[$0-4], _$[$0]));
break;
case 87:this.$ = new SwitchNode($$[$0-4], $$[$0-2], $$[$0-1].body, new Loc(_$[$0-5], _$[$0]));
break;
case 88:this.$ = [$$[$0]];
break;
case 89:this.$ = $$[$0-1]; this.$.push($$[$0]);
break;
case 90:this.$ = { value: $$[$0-1], block: $$[$0] };
break;
case 91:this.$ = new WithNode($$[$0-1], null, $$[$0], new Loc(_$[$0-2], _$[$0]));
break;
case 92:this.$ = new WithNode($$[$0-3], $$[$0-1]  , $$[$0], new Loc(_$[$0-4], _$[$0]));
break;
case 93:$$[$0-1].body.push($$[$0]); this.$ = $$[$0-1];
break;
case 94:$$[$0-1].body.push($$[$0]); this.$ = $$[$0-1];
break;
case 95:this.$ = new TextNode($$[$0].trimLeft(), false, new Loc(_$[$0], _$[$0]));
break;
case 96:this.$ = new TextNode($$[$0]           , true , new Loc(_$[$0], _$[$0]));
break;
case 97:this.$ = new StringNode($$[$0], new Loc(_$[$0], _$[$0]));
break;
case 98:
      this.$ = new StringNode($$[$0].slice(1), new Loc(_$[$0], _$[$0])); 
      console.log("Mañana WARNING: Interpolated strings are deprecated.");
      /*
      console.log("\tUse new-style strings (\"abc@{d}e\") without the \"i\"");
      console.log("\tIn VIM run: %s/=i\"/=\"/g");
      console.log("\tAt Command Line run: cd /dir/path && find . -type f -exec sed -i '' 's/=i\"/=\"/' {} \\;");
      */
    
break;
case 99:this.$ = new NameNode($$[$0-1], null, new Loc(_$[$0-2], _$[$0-2]));
break;
case 100:this.$ = new NameNode($$[$0-3], $$[$0-1]  , new Loc(_$[$0-4], _$[$0]));
break;
case 106:this.$ = new PathNode(null, $$[$0], new Loc(_$[$0], _$[$0]));
break;
case 107:this.$ = new PathNode($$[$0-2]  , $$[$0], new Loc(_$[$0-2], _$[$0]));
break;
case 108:this.$ = new IdNode($$[$0], null, null, new Loc(_$[$0], _$[$0]));
break;
case 109:this.$ = new IdNode($$[$0-3], $$[$0-1]  , null, new Loc(_$[$0-3], _$[$0]));
break;
case 110:this.$ = new IdNode($$[$0-5], $$[$0-3]  , $$[$0-1]  , new Loc(_$[$0-5], _$[$0]));
break;
case 111:this.$ = new IdNode($$[$0-5], $$[$0-3]  , $$[$0-1]  , new Loc(_$[$0-5], _$[$0]));
break;
case 112:this.$ = new IdNode($$[$0-3], $$[$0-1]  , null, new Loc(_$[$0-3], _$[$0]));
break;
case 113:this.$ = new IdNode($$[$0-5], $$[$0-3]  , $$[$0-1]  , new Loc(_$[$0-5], _$[$0]));
break;
case 114:this.$ = new IdNode($$[$0-5], $$[$0-3]  , $$[$0-1]  , new Loc(_$[$0-5], _$[$0]));
break;
case 115:this.$ = new IdNode($$[$0-4], '*' , $$[$0-1]  , new Loc(_$[$0-4], _$[$0]));
break;
case 116:this.$ = new IdNode($$[$0-4], $$[$0-2]  , '*' , new Loc(_$[$0-4], _$[$0]));
break;
case 117:this.$ = new IdNode($$[$0-3], $$[$0-1]  , null, new Loc(_$[$0-3], _$[$0]));
break;
case 118:this.$ = new FunctionNode($$[$0-1], null, new Loc(_$[$0-1], _$[$0]));
break;
case 119:this.$ = new FunctionNode($$[$0-2], $$[$0-1]  , new Loc(_$[$0-2], _$[$0]));
break;
case 120:this.$ = $$[$0-2]; this.$.push($$[$0]);
break;
case 121:this.$ = [$$[$0]];
break;
case 129:this.$ = $$[$0];
break;
case 130:this.$ = $$[$0];
break;
case 131:this.$ = $$[$0];
break;
case 132:this.$ = $$[$0];
break;
case 133:this.$ = $$[$0];
break;
case 134:this.$ = $$[$0];
break;
case 135:this.$ = $$[$0];
break;
case 138:this.$ = $$[$0] === "true";
break;
}
},
table: [{3:1,4:2,6:3,7:4,8:[1,20],11:5,12:[1,6],13:[1,7],15:8,16:9,17:10,18:11,19:12,20:13,21:14,22:15,23:16,24:[1,17],25:[1,18],26:[1,19],27:[1,21],30:[1,22],43:[1,31],48:23,49:32,52:[1,33],70:[1,26],74:[1,27],75:[1,28],76:[1,24],77:[1,29],81:[1,30],95:[1,25]},{1:[3]},{5:[1,34],6:35,11:5,12:[1,6],13:[1,7],15:8,16:9,17:10,18:11,19:12,20:13,21:14,22:15,23:16,24:[1,17],25:[1,18],26:[1,19],27:[1,21],30:[1,22],43:[1,31],48:23,49:32,52:[1,33],70:[1,26],74:[1,27],75:[1,28],76:[1,24],77:[1,29],81:[1,30],95:[1,25]},{5:[2,3],12:[2,3],13:[2,3],24:[2,3],25:[2,3],26:[2,3],27:[2,3],30:[2,3],43:[2,3],52:[2,3],70:[2,3],74:[2,3],75:[2,3],76:[2,3],77:[2,3],81:[2,3],95:[2,3]},{5:[2,4],12:[2,4],13:[2,4],24:[2,4],25:[2,4],26:[2,4],27:[2,4],30:[2,4],43:[2,4],52:[2,4],70:[2,4],74:[2,4],75:[2,4],76:[2,4],77:[2,4],81:[2,4],95:[2,4]},{5:[2,8],10:[2,8],12:[2,8],13:[2,8],24:[2,8],25:[2,8],26:[2,8],27:[2,8],30:[2,8],43:[2,8],52:[2,8],70:[2,8],74:[2,8],75:[2,8],76:[2,8],77:[2,8],81:[2,8],95:[2,8]},{5:[2,9],10:[2,9],12:[2,9],13:[2,9],24:[2,9],25:[2,9],26:[2,9],27:[2,9],30:[2,9],43:[2,9],52:[2,9],70:[2,9],74:[2,9],75:[2,9],76:[2,9],77:[2,9],81:[2,9],95:[2,9]},{14:36,82:[1,37],83:38,86:[1,39]},{5:[2,11],10:[2,11],12:[2,11],13:[2,11],24:[2,11],25:[2,11],26:[2,11],27:[2,11],30:[2,11],43:[2,11],52:[2,11],70:[2,11],74:[2,11],75:[2,11],76:[2,11],77:[2,11],81:[2,11],95:[2,11]},{5:[2,12],10:[2,12],12:[2,12],13:[2,12],24:[2,12],25:[2,12],26:[2,12],27:[2,12],30:[2,12],43:[2,12],52:[2,12],70:[2,12],74:[2,12],75:[2,12],76:[2,12],77:[2,12],81:[2,12],95:[2,12]},{5:[2,13],10:[2,13],12:[2,13],13:[2,13],24:[2,13],25:[2,13],26:[2,13],27:[2,13],30:[2,13],43:[2,13],52:[2,13],70:[2,13],74:[2,13],75:[2,13],76:[2,13],77:[2,13],81:[2,13],95:[2,13]},{5:[2,14],10:[2,14],12:[2,14],13:[2,14],24:[2,14],25:[2,14],26:[2,14],27:[2,14],30:[2,14],43:[2,14],52:[2,14],70:[2,14],74:[2,14],75:[2,14],76:[2,14],77:[2,14],81:[2,14],95:[2,14]},{5:[2,15],10:[2,15],12:[2,15],13:[2,15],24:[2,15],25:[2,15],26:[2,15],27:[2,15],30:[2,15],43:[2,15],52:[2,15],70:[2,15],74:[2,15],75:[2,15],76:[2,15],77:[2,15],81:[2,15],95:[2,15]},{5:[2,16],10:[2,16],12:[2,16],13:[2,16],24:[2,16],25:[2,16],26:[2,16],27:[2,16],30:[2,16],43:[2,16],52:[2,16],70:[2,16],74:[2,16],75:[2,16],76:[2,16],77:[2,16],81:[2,16],95:[2,16]},{5:[2,17],10:[2,17],12:[2,17],13:[2,17],24:[2,17],25:[2,17],26:[2,17],27:[2,17],30:[2,17],43:[2,17],52:[2,17],70:[2,17],74:[2,17],75:[2,17],76:[2,17],77:[2,17],81:[2,17],95:[2,17]},{5:[2,18],10:[2,18],12:[2,18],13:[2,18],24:[2,18],25:[2,18],26:[2,18],27:[2,18],30:[2,18],43:[2,18],52:[2,18],70:[2,18],74:[2,18],75:[2,18],76:[2,18],77:[2,18],81:[2,18],95:[2,18]},{5:[2,19],10:[2,19],12:[2,19],13:[2,19],24:[2,19],25:[2,19],26:[2,19],27:[2,19],30:[2,19],43:[2,19],52:[2,19],70:[2,19],74:[2,19],75:[2,19],76:[2,19],77:[2,19],81:[2,19],95:[2,19]},{8:[1,40]},{5:[2,21],10:[2,21],12:[2,21],13:[2,21],24:[2,21],25:[2,21],26:[2,21],27:[2,21],30:[2,21],43:[2,21],52:[2,21],70:[2,21],74:[2,21],75:[2,21],76:[2,21],77:[2,21],81:[2,21],95:[2,21]},{5:[2,22],10:[2,22],12:[2,22],13:[2,22],24:[2,22],25:[2,22],26:[2,22],27:[2,22],30:[2,22],43:[2,22],52:[2,22],70:[2,22],74:[2,22],75:[2,22],76:[2,22],77:[2,22],81:[2,22],95:[2,22]},{6:42,9:41,11:5,12:[1,6],13:[1,7],15:8,16:9,17:10,18:11,19:12,20:13,21:14,22:15,23:16,24:[1,17],25:[1,18],26:[1,19],27:[1,21],30:[1,22],43:[1,31],48:23,49:32,52:[1,33],70:[1,26],74:[1,27],75:[1,28],76:[1,24],77:[1,29],81:[1,30],95:[1,25]},{5:[2,31],7:45,8:[1,20],10:[2,31],12:[2,31],13:[2,31],14:46,24:[2,31],25:[2,31],26:[2,31],27:[2,31],28:43,29:44,30:[2,31],31:47,32:[1,51],33:[1,52],34:[1,53],35:[1,48],38:[1,49],39:[1,50],43:[2,31],52:[2,31],70:[2,31],74:[2,31],75:[2,31],76:[2,31],77:[2,31],81:[2,31],82:[1,37],83:38,86:[1,39],95:[2,31]},{5:[2,32],10:[2,32],12:[2,32],13:[2,32],24:[2,32],25:[2,32],26:[2,32],27:[2,32],28:54,29:55,30:[2,32],31:47,32:[1,51],33:[1,52],34:[1,53],35:[1,48],38:[1,49],39:[1,50],43:[2,32],52:[2,32],70:[2,32],74:[2,32],75:[2,32],76:[2,32],77:[2,32],81:[2,32],95:[2,32]},{5:[2,53],10:[2,53],12:[2,53],13:[2,53],24:[2,53],25:[2,53],26:[2,53],27:[2,53],30:[2,53],43:[2,53],52:[2,53],70:[2,53],74:[2,53],75:[2,53],76:[2,53],77:[2,53],81:[2,53],95:[2,53]},{73:[1,56]},{17:63,37:62,66:[1,66],67:60,68:[1,61],69:65,73:[1,67],84:[1,69],85:[1,70],90:68,95:[1,25],96:[1,57],97:58,98:59,99:[1,64],101:[1,71]},{17:74,67:73,71:72,73:[1,75],90:68,95:[1,25]},{73:[1,76]},{37:77,67:78,73:[1,75],84:[1,69],85:[1,70],90:68},{17:81,37:82,61:79,67:80,68:[1,83],69:84,73:[1,75],84:[1,69],85:[1,70],90:68,95:[1,25],101:[1,71]},{67:85,73:[1,75],90:68},{8:[1,89],42:[1,87],44:86,45:88,47:[1,90]},{5:[2,54],10:[2,54],12:[2,54],13:[2,54],24:[2,54],25:[2,54],26:[2,54],27:[2,54],30:[2,54],43:[2,54],50:91,51:92,52:[2,54],54:94,55:[1,95],56:[1,93],70:[2,54],74:[2,54],75:[2,54],76:[2,54],77:[2,54],81:[2,54],95:[2,54]},{17:81,37:82,53:96,57:97,60:[1,98],61:100,62:[1,99],67:80,68:[1,83],69:84,73:[1,75],84:[1,69],85:[1,70],90:68,95:[1,25],101:[1,71]},{1:[2,1]},{5:[2,2],12:[2,2],13:[2,2],24:[2,2],25:[2,2],26:[2,2],27:[2,2],30:[2,2],43:[2,2],52:[2,2],70:[2,2],74:[2,2],75:[2,2],76:[2,2],77:[2,2],81:[2,2],95:[2,2]},{5:[2,10],10:[2,10],12:[2,10],13:[2,10],24:[2,10],25:[2,10],26:[2,10],27:[2,10],30:[2,10],43:[2,10],52:[2,10],70:[2,10],74:[2,10],75:[2,10],76:[2,10],77:[2,10],81:[2,10],82:[1,101],83:102,86:[1,39],95:[2,10]},{5:[2,95],10:[2,95],12:[2,95],13:[2,95],24:[2,95],25:[2,95],26:[2,95],27:[2,95],30:[2,95],43:[2,95],52:[2,95],70:[2,95],74:[2,95],75:[2,95],76:[2,95],77:[2,95],81:[2,95],82:[2,95],86:[2,95],95:[2,95]},{5:[2,96],10:[2,96],12:[2,96],13:[2,96],24:[2,96],25:[2,96],26:[2,96],27:[2,96],30:[2,96],43:[2,96],52:[2,96],70:[2,96],74:[2,96],75:[2,96],76:[2,96],77:[2,96],81:[2,96],82:[2,96],86:[2,96],95:[2,96]},{67:103,73:[1,75],90:68},{14:104,82:[1,37],83:38,86:[1,39]},{6:106,10:[1,105],11:5,12:[1,6],13:[1,7],15:8,16:9,17:10,18:11,19:12,20:13,21:14,22:15,23:16,24:[1,17],25:[1,18],26:[1,19],27:[1,21],30:[1,22],43:[1,31],48:23,49:32,52:[1,33],70:[1,26],74:[1,27],75:[1,28],76:[1,24],77:[1,29],81:[1,30],95:[1,25]},{10:[2,7],12:[2,7],13:[2,7],24:[2,7],25:[2,7],26:[2,7],27:[2,7],30:[2,7],43:[2,7],52:[2,7],70:[2,7],74:[2,7],75:[2,7],76:[2,7],77:[2,7],81:[2,7],95:[2,7]},{5:[2,25],7:107,8:[1,20],10:[2,25],12:[2,25],13:[2,25],14:108,24:[2,25],25:[2,25],26:[2,25],27:[2,25],30:[2,25],31:109,32:[1,51],33:[1,52],34:[1,53],43:[2,25],52:[2,25],70:[2,25],74:[2,25],75:[2,25],76:[2,25],77:[2,25],81:[2,25],82:[1,37],83:38,86:[1,39],95:[2,25]},{5:[2,28],7:110,8:[1,20],10:[2,28],12:[2,28],13:[2,28],14:111,24:[2,28],25:[2,28],26:[2,28],27:[2,28],30:[2,28],35:[1,112],38:[1,113],43:[2,28],52:[2,28],70:[2,28],74:[2,28],75:[2,28],76:[2,28],77:[2,28],81:[2,28],82:[1,37],83:38,86:[1,39],95:[2,28]},{5:[2,29],10:[2,29],12:[2,29],13:[2,29],24:[2,29],25:[2,29],26:[2,29],27:[2,29],30:[2,29],43:[2,29],52:[2,29],70:[2,29],74:[2,29],75:[2,29],76:[2,29],77:[2,29],81:[2,29],95:[2,29]},{5:[2,30],10:[2,30],12:[2,30],13:[2,30],24:[2,30],25:[2,30],26:[2,30],27:[2,30],30:[2,30],43:[2,30],52:[2,30],70:[2,30],74:[2,30],75:[2,30],76:[2,30],77:[2,30],81:[2,30],82:[1,101],83:102,86:[1,39],95:[2,30]},{5:[2,36],8:[2,36],10:[2,36],12:[2,36],13:[2,36],24:[2,36],25:[2,36],26:[2,36],27:[2,36],30:[2,36],32:[2,36],33:[2,36],34:[2,36],43:[2,36],52:[2,36],70:[2,36],74:[2,36],75:[2,36],76:[2,36],77:[2,36],81:[2,36],82:[2,36],86:[2,36],95:[2,36]},{36:[1,114]},{36:[1,115]},{29:116,35:[1,48],38:[1,49],39:[1,50]},{5:[2,37],8:[2,37],10:[2,37],12:[2,37],13:[2,37],24:[2,37],25:[2,37],26:[2,37],27:[2,37],30:[2,37],32:[2,37],33:[2,37],34:[2,37],43:[2,37],52:[2,37],70:[2,37],74:[2,37],75:[2,37],76:[2,37],77:[2,37],81:[2,37],82:[2,37],86:[2,37],95:[2,37]},{5:[2,38],8:[2,38],10:[2,38],12:[2,38],13:[2,38],24:[2,38],25:[2,38],26:[2,38],27:[2,38],30:[2,38],32:[2,38],33:[2,38],34:[2,38],43:[2,38],52:[2,38],70:[2,38],74:[2,38],75:[2,38],76:[2,38],77:[2,38],81:[2,38],82:[2,38],86:[2,38],95:[2,38]},{5:[2,39],8:[2,39],10:[2,39],12:[2,39],13:[2,39],24:[2,39],25:[2,39],26:[2,39],27:[2,39],30:[2,39],32:[2,39],33:[2,39],34:[2,39],43:[2,39],52:[2,39],70:[2,39],74:[2,39],75:[2,39],76:[2,39],77:[2,39],81:[2,39],82:[2,39],86:[2,39],95:[2,39]},{5:[2,33],10:[2,33],12:[2,33],13:[2,33],24:[2,33],25:[2,33],26:[2,33],27:[2,33],30:[2,33],31:109,32:[1,51],33:[1,52],34:[1,53],43:[2,33],52:[2,33],70:[2,33],74:[2,33],75:[2,33],76:[2,33],77:[2,33],81:[2,33],95:[2,33]},{5:[2,34],10:[2,34],12:[2,34],13:[2,34],24:[2,34],25:[2,34],26:[2,34],27:[2,34],30:[2,34],35:[1,112],38:[1,113],43:[2,34],52:[2,34],70:[2,34],74:[2,34],75:[2,34],76:[2,34],77:[2,34],81:[2,34],95:[2,34]},{64:[1,117]},{5:[2,118],8:[2,118],10:[2,118],12:[2,118],13:[2,118],24:[2,118],25:[2,118],26:[2,118],27:[2,118],30:[2,118],43:[2,118],52:[2,118],58:[2,118],59:[2,118],62:[2,118],63:[2,118],64:[2,118],65:[2,118],70:[2,118],72:[2,118],74:[2,118],75:[2,118],76:[2,118],77:[2,118],81:[2,118],87:[2,118],88:[2,118],95:[2,118],96:[2,118]},{88:[1,119],96:[1,118]},{88:[2,121],96:[2,121]},{88:[2,122],91:[1,120],96:[2,122]},{88:[2,123],96:[2,123]},{88:[2,124],96:[2,124]},{88:[2,125],96:[2,125]},{88:[2,126],96:[2,126]},{88:[2,127],96:[2,127]},{88:[2,128],96:[2,128]},{88:[2,108],91:[2,108],92:[1,122],96:[2,108],100:[1,121]},{5:[2,106],8:[2,106],10:[2,106],12:[2,106],13:[2,106],24:[2,106],25:[2,106],26:[2,106],27:[2,106],30:[2,106],43:[2,106],52:[2,106],58:[2,106],59:[2,106],62:[2,106],63:[2,106],64:[2,106],65:[2,106],70:[2,106],72:[2,106],74:[2,106],75:[2,106],76:[2,106],77:[2,106],81:[2,106],87:[2,106],88:[2,106],91:[2,106],93:[2,106],94:[2,106],95:[2,106],96:[2,106]},{5:[2,97],8:[2,97],10:[2,97],12:[2,97],13:[2,97],24:[2,97],25:[2,97],26:[2,97],27:[2,97],30:[2,97],35:[2,97],38:[2,97],40:[2,97],43:[2,97],52:[2,97],58:[2,97],59:[2,97],62:[2,97],63:[2,97],64:[2,97],65:[2,97],70:[2,97],74:[2,97],75:[2,97],76:[2,97],77:[2,97],81:[2,97],82:[2,97],86:[2,97],87:[2,97],88:[2,97],93:[2,97],95:[2,97],96:[2,97]},{5:[2,98],8:[2,98],10:[2,98],12:[2,98],13:[2,98],24:[2,98],25:[2,98],26:[2,98],27:[2,98],30:[2,98],35:[2,98],38:[2,98],40:[2,98],43:[2,98],52:[2,98],58:[2,98],59:[2,98],62:[2,98],63:[2,98],64:[2,98],65:[2,98],70:[2,98],74:[2,98],75:[2,98],76:[2,98],77:[2,98],81:[2,98],82:[2,98],86:[2,98],87:[2,98],88:[2,98],93:[2,98],95:[2,98],96:[2,98]},{8:[2,138],58:[2,138],59:[2,138],62:[2,138],63:[2,138],64:[2,138],65:[2,138],87:[2,138],88:[2,138],96:[2,138]},{72:[1,123]},{72:[2,136],91:[1,120]},{72:[2,137]},{5:[2,108],8:[2,108],10:[2,108],12:[2,108],13:[2,108],24:[2,108],25:[2,108],26:[2,108],27:[2,108],30:[2,108],43:[2,108],52:[2,108],58:[2,108],59:[2,108],62:[2,108],63:[2,108],64:[2,108],65:[2,108],70:[2,108],72:[2,108],74:[2,108],75:[2,108],76:[2,108],77:[2,108],81:[2,108],87:[2,108],88:[2,108],91:[2,108],92:[1,122],93:[2,108],94:[2,108],95:[2,108],96:[2,108]},{5:[2,81],10:[2,81],12:[2,81],13:[2,81],24:[2,81],25:[2,81],26:[2,81],27:[2,81],30:[2,81],43:[2,81],52:[2,81],70:[2,81],74:[2,81],75:[2,81],76:[2,81],77:[2,81],81:[2,81],95:[2,81]},{5:[2,82],10:[2,82],12:[2,82],13:[2,82],24:[2,82],25:[2,82],26:[2,82],27:[2,82],30:[2,82],43:[2,82],52:[2,82],70:[2,82],74:[2,82],75:[2,82],76:[2,82],77:[2,82],81:[2,82],95:[2,82]},{5:[2,83],10:[2,83],12:[2,83],13:[2,83],24:[2,83],25:[2,83],26:[2,83],27:[2,83],30:[2,83],43:[2,83],52:[2,83],70:[2,83],74:[2,83],75:[2,83],76:[2,83],77:[2,83],81:[2,83],91:[1,120],95:[2,83]},{8:[1,124]},{8:[2,75],58:[2,75],59:[2,75],62:[2,75],63:[2,75],64:[2,75],65:[2,75],91:[1,120]},{8:[2,76],58:[2,76],59:[2,76],62:[2,76],63:[2,76],64:[2,76],65:[2,76]},{8:[2,77],58:[2,77],59:[2,77],62:[2,77],63:[2,77],64:[2,77],65:[2,77]},{8:[2,78],58:[2,78],59:[2,78],62:[2,78],63:[2,78],64:[2,78],65:[2,78]},{8:[2,79],58:[2,79],59:[2,79],62:[2,79],63:[2,79],64:[2,79],65:[2,79]},{7:125,8:[1,20],72:[1,126],91:[1,120]},{8:[1,89],10:[1,127],45:128,47:[1,90]},{8:[1,89],44:129,45:88,47:[1,90]},{8:[2,50],10:[2,50],47:[2,50]},{46:[1,130]},{8:[2,52],10:[2,52],47:[2,52]},{5:[2,55],10:[2,55],12:[2,55],13:[2,55],24:[2,55],25:[2,55],26:[2,55],27:[2,55],30:[2,55],43:[2,55],52:[2,55],70:[2,55],74:[2,55],75:[2,55],76:[2,55],77:[2,55],81:[2,55],95:[2,55]},{5:[2,56],10:[2,56],12:[2,56],13:[2,56],24:[2,56],25:[2,56],26:[2,56],27:[2,56],30:[2,56],43:[2,56],50:131,52:[2,56],54:132,55:[1,95],56:[1,93],70:[2,56],74:[2,56],75:[2,56],76:[2,56],77:[2,56],81:[2,56],95:[2,56]},{7:133,8:[1,20]},{5:[2,59],10:[2,59],12:[2,59],13:[2,59],24:[2,59],25:[2,59],26:[2,59],27:[2,59],30:[2,59],43:[2,59],52:[2,59],55:[2,59],56:[2,59],70:[2,59],74:[2,59],75:[2,59],76:[2,59],77:[2,59],81:[2,59],95:[2,59]},{17:81,37:82,53:134,57:97,60:[1,98],61:100,62:[1,99],67:80,68:[1,83],69:84,73:[1,75],84:[1,69],85:[1,70],90:68,95:[1,25],101:[1,71]},{7:135,8:[1,20],58:[1,136],59:[1,137]},{8:[2,63],58:[2,63],59:[2,63]},{17:81,37:82,61:138,67:80,68:[1,83],69:84,73:[1,75],84:[1,69],85:[1,70],90:68,95:[1,25],101:[1,71]},{17:81,37:82,60:[1,139],61:140,67:80,68:[1,83],69:84,73:[1,75],84:[1,69],85:[1,70],90:68,95:[1,25],101:[1,71]},{8:[2,69],58:[2,69],59:[2,69],62:[1,143],63:[1,141],64:[1,142],65:[1,144]},{5:[2,93],10:[2,93],12:[2,93],13:[2,93],24:[2,93],25:[2,93],26:[2,93],27:[2,93],30:[2,93],43:[2,93],52:[2,93],70:[2,93],74:[2,93],75:[2,93],76:[2,93],77:[2,93],81:[2,93],82:[2,93],86:[2,93],95:[2,93]},{5:[2,94],10:[2,94],12:[2,94],13:[2,94],24:[2,94],25:[2,94],26:[2,94],27:[2,94],30:[2,94],43:[2,94],52:[2,94],70:[2,94],74:[2,94],75:[2,94],76:[2,94],77:[2,94],81:[2,94],82:[2,94],86:[2,94],95:[2,94]},{87:[1,145],88:[1,146],91:[1,120]},{10:[1,147],82:[1,101],83:102,86:[1,39]},{5:[2,5],10:[2,5],12:[2,5],13:[2,5],24:[2,5],25:[2,5],26:[2,5],27:[2,5],30:[2,5],43:[2,5],52:[2,5],55:[2,5],56:[2,5],70:[2,5],74:[2,5],75:[2,5],76:[2,5],77:[2,5],80:[2,5],81:[2,5],95:[2,5]},{10:[2,6],12:[2,6],13:[2,6],24:[2,6],25:[2,6],26:[2,6],27:[2,6],30:[2,6],43:[2,6],52:[2,6],70:[2,6],74:[2,6],75:[2,6],76:[2,6],77:[2,6],81:[2,6],95:[2,6]},{5:[2,23],10:[2,23],12:[2,23],13:[2,23],24:[2,23],25:[2,23],26:[2,23],27:[2,23],30:[2,23],43:[2,23],52:[2,23],70:[2,23],74:[2,23],75:[2,23],76:[2,23],77:[2,23],81:[2,23],95:[2,23]},{5:[2,24],10:[2,24],12:[2,24],13:[2,24],24:[2,24],25:[2,24],26:[2,24],27:[2,24],30:[2,24],43:[2,24],52:[2,24],70:[2,24],74:[2,24],75:[2,24],76:[2,24],77:[2,24],81:[2,24],82:[1,101],83:102,86:[1,39],95:[2,24]},{5:[2,35],8:[2,35],10:[2,35],12:[2,35],13:[2,35],24:[2,35],25:[2,35],26:[2,35],27:[2,35],30:[2,35],32:[2,35],33:[2,35],34:[2,35],43:[2,35],52:[2,35],70:[2,35],74:[2,35],75:[2,35],76:[2,35],77:[2,35],81:[2,35],82:[2,35],86:[2,35],95:[2,35]},{5:[2,26],10:[2,26],12:[2,26],13:[2,26],24:[2,26],25:[2,26],26:[2,26],27:[2,26],30:[2,26],43:[2,26],52:[2,26],70:[2,26],74:[2,26],75:[2,26],76:[2,26],77:[2,26],81:[2,26],95:[2,26]},{5:[2,27],10:[2,27],12:[2,27],13:[2,27],24:[2,27],25:[2,27],26:[2,27],27:[2,27],30:[2,27],43:[2,27],52:[2,27],70:[2,27],74:[2,27],75:[2,27],76:[2,27],77:[2,27],81:[2,27],82:[1,101],83:102,86:[1,39],95:[2,27]},{36:[1,148]},{36:[1,149]},{37:150,84:[1,69],85:[1,70]},{37:151,84:[1,69],85:[1,70]},{35:[1,112],38:[1,113],40:[1,152]},{37:154,67:153,73:[1,75],84:[1,69],85:[1,70],90:68},{5:[2,119],8:[2,119],10:[2,119],12:[2,119],13:[2,119],24:[2,119],25:[2,119],26:[2,119],27:[2,119],30:[2,119],43:[2,119],52:[2,119],58:[2,119],59:[2,119],62:[2,119],63:[2,119],64:[2,119],65:[2,119],70:[2,119],72:[2,119],74:[2,119],75:[2,119],76:[2,119],77:[2,119],81:[2,119],87:[2,119],88:[2,119],95:[2,119],96:[2,119]},{17:63,37:62,66:[1,66],67:60,68:[1,61],69:65,73:[1,67],84:[1,69],85:[1,70],90:68,95:[1,25],98:155,99:[1,64],101:[1,71]},{73:[1,75],90:156},{17:160,37:159,66:[1,163],67:157,68:[1,158],69:162,73:[1,75],84:[1,69],85:[1,70],90:68,95:[1,25],99:[1,161],101:[1,71]},{37:167,67:165,68:[1,164],73:[1,75],84:[1,69],85:[1,70],90:68,94:[1,166]},{73:[1,168]},{78:169,79:170,80:[1,171]},{5:[2,91],10:[2,91],12:[2,91],13:[2,91],24:[2,91],25:[2,91],26:[2,91],27:[2,91],30:[2,91],43:[2,91],52:[2,91],70:[2,91],74:[2,91],75:[2,91],76:[2,91],77:[2,91],81:[2,91],95:[2,91]},{73:[1,172]},{5:[2,47],10:[2,47],12:[2,47],13:[2,47],24:[2,47],25:[2,47],26:[2,47],27:[2,47],30:[2,47],43:[2,47],52:[2,47],70:[2,47],74:[2,47],75:[2,47],76:[2,47],77:[2,47],81:[2,47],95:[2,47]},{8:[2,49],10:[2,49],47:[2,49]},{8:[1,89],10:[1,173],45:128,47:[1,90]},{8:[2,51],10:[2,51],47:[2,51]},{5:[2,57],10:[2,57],12:[2,57],13:[2,57],24:[2,57],25:[2,57],26:[2,57],27:[2,57],30:[2,57],43:[2,57],52:[2,57],70:[2,57],74:[2,57],75:[2,57],76:[2,57],77:[2,57],81:[2,57],95:[2,57]},{5:[2,60],10:[2,60],12:[2,60],13:[2,60],24:[2,60],25:[2,60],26:[2,60],27:[2,60],30:[2,60],43:[2,60],52:[2,60],55:[2,60],56:[2,60],70:[2,60],74:[2,60],75:[2,60],76:[2,60],77:[2,60],81:[2,60],95:[2,60]},{5:[2,62],10:[2,62],12:[2,62],13:[2,62],24:[2,62],25:[2,62],26:[2,62],27:[2,62],30:[2,62],43:[2,62],52:[2,62],70:[2,62],74:[2,62],75:[2,62],76:[2,62],77:[2,62],81:[2,62],95:[2,62]},{7:174,8:[1,20],58:[1,136],59:[1,137]},{5:[2,58],10:[2,58],12:[2,58],13:[2,58],24:[2,58],25:[2,58],26:[2,58],27:[2,58],30:[2,58],43:[2,58],52:[2,58],55:[2,58],56:[2,58],70:[2,58],74:[2,58],75:[2,58],76:[2,58],77:[2,58],81:[2,58],95:[2,58]},{17:81,37:82,57:175,60:[1,98],61:100,62:[1,99],67:80,68:[1,83],69:84,73:[1,75],84:[1,69],85:[1,70],90:68,95:[1,25],101:[1,71]},{17:81,37:82,57:176,60:[1,98],61:100,62:[1,99],67:80,68:[1,83],69:84,73:[1,75],84:[1,69],85:[1,70],90:68,95:[1,25],101:[1,71]},{8:[2,66],58:[2,66],59:[2,66]},{17:81,37:82,61:177,67:80,68:[1,83],69:84,73:[1,75],84:[1,69],85:[1,70],90:68,95:[1,25],101:[1,71]},{8:[2,70],58:[2,70],59:[2,70]},{17:81,37:82,61:178,67:80,68:[1,83],69:84,73:[1,75],84:[1,69],85:[1,70],90:68,95:[1,25],101:[1,71]},{17:81,37:82,61:179,67:80,68:[1,83],69:84,73:[1,75],84:[1,69],85:[1,70],90:68,95:[1,25],101:[1,71]},{64:[1,180],65:[1,181]},{66:[1,182]},{5:[2,99],10:[2,99],12:[2,99],13:[2,99],24:[2,99],25:[2,99],26:[2,99],27:[2,99],30:[2,99],43:[2,99],52:[2,99],70:[2,99],74:[2,99],75:[2,99],76:[2,99],77:[2,99],81:[2,99],82:[2,99],86:[2,99],95:[2,99]},{17:188,37:184,67:187,68:[1,185],69:186,73:[1,75],84:[1,69],85:[1,70],89:183,90:68,95:[1,25],101:[1,71]},{5:[2,20],10:[2,20],12:[2,20],13:[2,20],24:[2,20],25:[2,20],26:[2,20],27:[2,20],30:[2,20],43:[2,20],52:[2,20],70:[2,20],74:[2,20],75:[2,20],76:[2,20],77:[2,20],81:[2,20],95:[2,20]},{37:189,84:[1,69],85:[1,70]},{37:190,84:[1,69],85:[1,70]},{5:[2,40],8:[2,40],10:[2,40],12:[2,40],13:[2,40],24:[2,40],25:[2,40],26:[2,40],27:[2,40],30:[2,40],35:[2,40],38:[2,40],40:[2,40],43:[2,40],52:[2,40],70:[2,40],74:[2,40],75:[2,40],76:[2,40],77:[2,40],81:[2,40],82:[2,40],86:[2,40],95:[2,40]},{5:[2,41],8:[2,41],10:[2,41],12:[2,41],13:[2,41],24:[2,41],25:[2,41],26:[2,41],27:[2,41],30:[2,41],35:[2,41],38:[2,41],40:[2,41],43:[2,41],52:[2,41],70:[2,41],74:[2,41],75:[2,41],76:[2,41],77:[2,41],81:[2,41],82:[2,41],86:[2,41],95:[2,41]},{5:[2,44],8:[2,44],10:[2,44],12:[2,44],13:[2,44],24:[2,44],25:[2,44],26:[2,44],27:[2,44],30:[2,44],35:[2,44],38:[2,44],40:[2,44],43:[2,44],52:[2,44],70:[2,44],74:[2,44],75:[2,44],76:[2,44],77:[2,44],81:[2,44],82:[2,44],86:[2,44],95:[2,44]},{7:191,8:[1,20],91:[1,120]},{7:192,8:[1,20]},{88:[2,120],96:[2,120]},{5:[2,107],8:[2,107],10:[2,107],12:[2,107],13:[2,107],24:[2,107],25:[2,107],26:[2,107],27:[2,107],30:[2,107],43:[2,107],52:[2,107],58:[2,107],59:[2,107],62:[2,107],63:[2,107],64:[2,107],65:[2,107],70:[2,107],72:[2,107],74:[2,107],75:[2,107],76:[2,107],77:[2,107],81:[2,107],87:[2,107],88:[2,107],91:[2,107],93:[2,107],94:[2,107],95:[2,107],96:[2,107]},{88:[2,129],91:[1,120],96:[2,129]},{88:[2,130],96:[2,130]},{88:[2,131],96:[2,131]},{88:[2,132],96:[2,132]},{88:[2,133],96:[2,133]},{88:[2,134],96:[2,134]},{88:[2,135],96:[2,135]},{93:[1,193],94:[1,194]},{91:[1,120],93:[1,195],94:[1,196]},{68:[1,197]},{93:[1,198]},{5:[2,80],10:[2,80],12:[2,80],13:[2,80],24:[2,80],25:[2,80],26:[2,80],27:[2,80],30:[2,80],43:[2,80],52:[2,80],70:[2,80],74:[2,80],75:[2,80],76:[2,80],77:[2,80],81:[2,80],95:[2,80]},{10:[1,199],50:200,56:[1,93],79:201,80:[1,171]},{10:[2,88],56:[2,88],80:[2,88]},{17:81,37:82,61:202,67:80,68:[1,83],69:84,73:[1,75],84:[1,69],85:[1,70],90:68,95:[1,25],101:[1,71]},{7:203,8:[1,20]},{5:[2,48],10:[2,48],12:[2,48],13:[2,48],24:[2,48],25:[2,48],26:[2,48],27:[2,48],30:[2,48],43:[2,48],52:[2,48],70:[2,48],74:[2,48],75:[2,48],76:[2,48],77:[2,48],81:[2,48],95:[2,48]},{5:[2,61],10:[2,61],12:[2,61],13:[2,61],24:[2,61],25:[2,61],26:[2,61],27:[2,61],30:[2,61],43:[2,61],52:[2,61],55:[2,61],56:[2,61],70:[2,61],74:[2,61],75:[2,61],76:[2,61],77:[2,61],81:[2,61],95:[2,61]},{8:[2,64],58:[2,64],59:[2,64]},{8:[2,65],58:[2,65],59:[2,65]},{8:[2,67],58:[2,67],59:[2,67]},{8:[2,68],58:[2,68],59:[2,68]},{8:[2,71],58:[2,71],59:[2,71]},{17:81,37:82,61:204,67:80,68:[1,83],69:84,73:[1,75],84:[1,69],85:[1,70],90:68,95:[1,25],101:[1,71]},{66:[1,205]},{8:[2,73],58:[2,73],59:[2,73]},{87:[1,206]},{87:[2,101]},{87:[2,102]},{87:[2,103]},{87:[2,104],91:[1,120]},{87:[2,105]},{5:[2,42],8:[2,42],10:[2,42],12:[2,42],13:[2,42],24:[2,42],25:[2,42],26:[2,42],27:[2,42],30:[2,42],35:[2,42],38:[2,42],40:[2,42],43:[2,42],52:[2,42],70:[2,42],74:[2,42],75:[2,42],76:[2,42],77:[2,42],81:[2,42],82:[2,42],86:[2,42],95:[2,42]},{5:[2,43],8:[2,43],10:[2,43],12:[2,43],13:[2,43],24:[2,43],25:[2,43],26:[2,43],27:[2,43],30:[2,43],35:[2,43],38:[2,43],40:[2,43],43:[2,43],52:[2,43],70:[2,43],74:[2,43],75:[2,43],76:[2,43],77:[2,43],81:[2,43],82:[2,43],86:[2,43],95:[2,43]},{5:[2,84],10:[2,84],12:[2,84],13:[2,84],24:[2,84],25:[2,84],26:[2,84],27:[2,84],30:[2,84],43:[2,84],52:[2,84],70:[2,84],74:[2,84],75:[2,84],76:[2,84],77:[2,84],81:[2,84],95:[2,84]},{5:[2,85],10:[2,85],12:[2,85],13:[2,85],24:[2,85],25:[2,85],26:[2,85],27:[2,85],30:[2,85],43:[2,85],52:[2,85],70:[2,85],74:[2,85],75:[2,85],76:[2,85],77:[2,85],81:[2,85],95:[2,85]},{5:[2,109],8:[2,109],10:[2,109],12:[2,109],13:[2,109],24:[2,109],25:[2,109],26:[2,109],27:[2,109],30:[2,109],43:[2,109],52:[2,109],58:[2,109],59:[2,109],62:[2,109],63:[2,109],64:[2,109],65:[2,109],70:[2,109],72:[2,109],74:[2,109],75:[2,109],76:[2,109],77:[2,109],81:[2,109],87:[2,109],88:[2,109],91:[2,109],93:[2,109],94:[2,109],95:[2,109],96:[2,109]},{67:208,68:[1,207],73:[1,75],90:68,93:[1,209]},{5:[2,112],8:[2,112],10:[2,112],12:[2,112],13:[2,112],24:[2,112],25:[2,112],26:[2,112],27:[2,112],30:[2,112],43:[2,112],52:[2,112],58:[2,112],59:[2,112],62:[2,112],63:[2,112],64:[2,112],65:[2,112],70:[2,112],72:[2,112],74:[2,112],75:[2,112],76:[2,112],77:[2,112],81:[2,112],87:[2,112],88:[2,112],91:[2,112],93:[2,112],94:[2,112],95:[2,112],96:[2,112]},{67:211,68:[1,210],73:[1,75],90:68},{93:[1,212]},{5:[2,117],8:[2,117],10:[2,117],12:[2,117],13:[2,117],24:[2,117],25:[2,117],26:[2,117],27:[2,117],30:[2,117],43:[2,117],52:[2,117],58:[2,117],59:[2,117],62:[2,117],63:[2,117],64:[2,117],65:[2,117],70:[2,117],72:[2,117],74:[2,117],75:[2,117],76:[2,117],77:[2,117],81:[2,117],87:[2,117],88:[2,117],91:[2,117],93:[2,117],94:[2,117],95:[2,117],96:[2,117]},{5:[2,86],10:[2,86],12:[2,86],13:[2,86],24:[2,86],25:[2,86],26:[2,86],27:[2,86],30:[2,86],43:[2,86],52:[2,86],70:[2,86],74:[2,86],75:[2,86],76:[2,86],77:[2,86],81:[2,86],95:[2,86]},{10:[1,213]},{10:[2,89],56:[2,89],80:[2,89]},{7:214,8:[1,20]},{5:[2,92],10:[2,92],12:[2,92],13:[2,92],24:[2,92],25:[2,92],26:[2,92],27:[2,92],30:[2,92],43:[2,92],52:[2,92],70:[2,92],74:[2,92],75:[2,92],76:[2,92],77:[2,92],81:[2,92],95:[2,92]},{8:[2,72],58:[2,72],59:[2,72]},{8:[2,74],58:[2,74],59:[2,74]},{5:[2,100],10:[2,100],12:[2,100],13:[2,100],24:[2,100],25:[2,100],26:[2,100],27:[2,100],30:[2,100],43:[2,100],52:[2,100],70:[2,100],74:[2,100],75:[2,100],76:[2,100],77:[2,100],81:[2,100],82:[2,100],86:[2,100],95:[2,100]},{93:[1,215]},{91:[1,120],93:[1,216]},{5:[2,116],8:[2,116],10:[2,116],12:[2,116],13:[2,116],24:[2,116],25:[2,116],26:[2,116],27:[2,116],30:[2,116],43:[2,116],52:[2,116],58:[2,116],59:[2,116],62:[2,116],63:[2,116],64:[2,116],65:[2,116],70:[2,116],72:[2,116],74:[2,116],75:[2,116],76:[2,116],77:[2,116],81:[2,116],87:[2,116],88:[2,116],91:[2,116],93:[2,116],94:[2,116],95:[2,116],96:[2,116]},{93:[1,217]},{91:[1,120],93:[1,218]},{5:[2,115],8:[2,115],10:[2,115],12:[2,115],13:[2,115],24:[2,115],25:[2,115],26:[2,115],27:[2,115],30:[2,115],43:[2,115],52:[2,115],58:[2,115],59:[2,115],62:[2,115],63:[2,115],64:[2,115],65:[2,115],70:[2,115],72:[2,115],74:[2,115],75:[2,115],76:[2,115],77:[2,115],81:[2,115],87:[2,115],88:[2,115],91:[2,115],93:[2,115],94:[2,115],95:[2,115],96:[2,115]},{5:[2,87],10:[2,87],12:[2,87],13:[2,87],24:[2,87],25:[2,87],26:[2,87],27:[2,87],30:[2,87],43:[2,87],52:[2,87],70:[2,87],74:[2,87],75:[2,87],76:[2,87],77:[2,87],81:[2,87],95:[2,87]},{10:[2,90],56:[2,90],80:[2,90]},{5:[2,110],8:[2,110],10:[2,110],12:[2,110],13:[2,110],24:[2,110],25:[2,110],26:[2,110],27:[2,110],30:[2,110],43:[2,110],52:[2,110],58:[2,110],59:[2,110],62:[2,110],63:[2,110],64:[2,110],65:[2,110],70:[2,110],72:[2,110],74:[2,110],75:[2,110],76:[2,110],77:[2,110],81:[2,110],87:[2,110],88:[2,110],91:[2,110],93:[2,110],94:[2,110],95:[2,110],96:[2,110]},{5:[2,111],8:[2,111],10:[2,111],12:[2,111],13:[2,111],24:[2,111],25:[2,111],26:[2,111],27:[2,111],30:[2,111],43:[2,111],52:[2,111],58:[2,111],59:[2,111],62:[2,111],63:[2,111],64:[2,111],65:[2,111],70:[2,111],72:[2,111],74:[2,111],75:[2,111],76:[2,111],77:[2,111],81:[2,111],87:[2,111],88:[2,111],91:[2,111],93:[2,111],94:[2,111],95:[2,111],96:[2,111]},{5:[2,113],8:[2,113],10:[2,113],12:[2,113],13:[2,113],24:[2,113],25:[2,113],26:[2,113],27:[2,113],30:[2,113],43:[2,113],52:[2,113],58:[2,113],59:[2,113],62:[2,113],63:[2,113],64:[2,113],65:[2,113],70:[2,113],72:[2,113],74:[2,113],75:[2,113],76:[2,113],77:[2,113],81:[2,113],87:[2,113],88:[2,113],91:[2,113],93:[2,113],94:[2,113],95:[2,113],96:[2,113]},{5:[2,114],8:[2,114],10:[2,114],12:[2,114],13:[2,114],24:[2,114],25:[2,114],26:[2,114],27:[2,114],30:[2,114],43:[2,114],52:[2,114],58:[2,114],59:[2,114],62:[2,114],63:[2,114],64:[2,114],65:[2,114],70:[2,114],72:[2,114],74:[2,114],75:[2,114],76:[2,114],77:[2,114],81:[2,114],87:[2,114],88:[2,114],91:[2,114],93:[2,114],94:[2,114],95:[2,114],96:[2,114]}],
defaultActions: {34:[2,1],74:[2,137],184:[2,101],185:[2,102],186:[2,103],188:[2,105]},
parseError: function parseError(str, hash) {
    if (hash.recoverable) {
        this.trace(str);
    } else {
        throw new Error(str);
    }
},
parse: function parse(input) {
    var self = this, stack = [0], tstack = [], vstack = [null], lstack = [], table = this.table, yytext = '', yylineno = 0, yyleng = 0, recovering = 0, TERROR = 2, EOF = 1;
    var args = lstack.slice.call(arguments, 1);
    this.lexer.setInput(input);
    this.lexer.yy = this.yy;
    this.yy.lexer = this.lexer;
    this.yy.parser = this;
    if (typeof this.lexer.yylloc == 'undefined') {
        this.lexer.yylloc = {};
    }
    var yyloc = this.lexer.yylloc;
    lstack.push(yyloc);
    var ranges = this.lexer.options && this.lexer.options.ranges;
    if (typeof this.yy.parseError === 'function') {
        this.parseError = this.yy.parseError;
    } else {
        this.parseError = Object.getPrototypeOf(this).parseError;
    }
    function popStack(n) {
        stack.length = stack.length - 2 * n;
        vstack.length = vstack.length - n;
        lstack.length = lstack.length - n;
    }
    function lex() {
        var token;
        token = tstack.shift() || self.lexer.lex() || EOF;
        if (typeof token !== 'number') {
            if (token instanceof Array) {
                tstack = tstack.concat(token.splice(1));
                token = token[0];
            }
            token = self.symbols_[token] || token;
        }
        return token;
    }
    var symbol, preErrorSymbol, state, action, a, r, yyval = {}, p, len, newState, expected;
    while (true) {
        state = stack[stack.length - 1];
        if (this.defaultActions[state]) {
            action = this.defaultActions[state];
        } else {
            if (symbol === null || typeof symbol == 'undefined') {
                symbol = lex();
            }
            action = table[state] && table[state][symbol];
        }
                    if (typeof action === 'undefined' || !action.length || !action[0]) {
                var errStr = '';
                expected = [];
                for (p in table[state]) {
                    if (this.terminals_[p] && p > TERROR) {
                        expected.push('\'' + this.terminals_[p] + '\'');
                    }
                }
                if (this.lexer.showPosition) {
                    errStr = 'Parse error on line ' + (yylineno + 1) + ':\n' + this.lexer.showPosition() + '\nExpecting ' + expected.join(', ') + ', got \'' + (this.terminals_[symbol] || symbol) + '\'';
                } else {
                    errStr = 'Parse error on line ' + (yylineno + 1) + ': Unexpected ' + (symbol == EOF ? 'end of input' : '\'' + (this.terminals_[symbol] || symbol) + '\'');
                }
                this.parseError(errStr, {
                    text: this.lexer.match,
                    token: this.terminals_[symbol] || symbol,
                    line: this.lexer.yylineno,
                    loc: yyloc,
                    expected: expected
                });
            }
        if (action[0] instanceof Array && action.length > 1) {
            throw new Error('Parse Error: multiple actions possible at state: ' + state + ', token: ' + symbol);
        }
        switch (action[0]) {
        case 1:
            stack.push(symbol);
            vstack.push(this.lexer.yytext);
            lstack.push(this.lexer.yylloc);
            stack.push(action[1]);
            symbol = null;
            if (!preErrorSymbol) {
                yyleng = this.lexer.yyleng;
                yytext = this.lexer.yytext;
                yylineno = this.lexer.yylineno;
                yyloc = this.lexer.yylloc;
                if (recovering > 0) {
                    recovering--;
                }
            } else {
                symbol = preErrorSymbol;
                preErrorSymbol = null;
            }
            break;
        case 2:
            len = this.productions_[action[1]][1];
            yyval.$ = vstack[vstack.length - len];
            yyval._$ = {
                first_line: lstack[lstack.length - (len || 1)].first_line,
                last_line: lstack[lstack.length - 1].last_line,
                first_column: lstack[lstack.length - (len || 1)].first_column,
                last_column: lstack[lstack.length - 1].last_column
            };
            if (ranges) {
                yyval._$.range = [
                    lstack[lstack.length - (len || 1)].range[0],
                    lstack[lstack.length - 1].range[1]
                ];
            }
            r = this.performAction.apply(yyval, [
                yytext,
                yyleng,
                yylineno,
                this.yy,
                action[1],
                vstack,
                lstack
            ].concat(args));
            if (typeof r !== 'undefined') {
                return r;
            }
            if (len) {
                stack = stack.slice(0, -1 * len * 2);
                vstack = vstack.slice(0, -1 * len);
                lstack = lstack.slice(0, -1 * len);
            }
            stack.push(this.productions_[action[1]][0]);
            vstack.push(yyval.$);
            lstack.push(yyval._$);
            newState = table[stack[stack.length - 2]][stack[stack.length - 1]];
            stack.push(newState);
            break;
        case 3:
            return true;
        }
    }
    return true;
}};


function Loc(start, end) {
  this.start = { line: start.first_line, column: start.first_column };
  this.end = { line: end.last_line, column: end.last_column };
}

function VoidTagNode(tag, attrs, loc) {
  this.type = "VoidTag";
  this.tag = tag;
  this.attrs = attrs;
  this.loc = loc;
}

function TagNode(tag, attrs, body, loc) {
  this.type = "Tag";
  this.tag = tag;
  this.attrs = attrs;
  this.body = body;
  this.loc = loc;
}

function CodeTagNode(tag, attrs, code, loc) {
  this.type = "CodeTag";
  this.tag = tag;
  this.attrs = attrs;
  this.body = code;
  this.loc = loc;
}

function TextNode(el, _is_name, loc) {
  this.type = "Text";
  this.loc = loc;

  if (_is_name && (el[0] === ' ' || el[0] === '\t')) {
    el = el.trimLeft();
  }

  this.body = [el]; 
}

function NameNode(path, default_value, loc) {
  this.type = "Name";
  this.path = path;
  this.default_value = default_value;
  this.loc = loc;
}

function IdNode(id, start, end, loc) {
  this.type = "Id";
  this.id = id;
  this.start = start;
  this.end = end;
  this.loc = loc;
}

function PathNode(path_node, component, loc) {
  var k, c;

  if (path_node === null) {
    this.type = "Path";
    this.loc = loc;
    this.components = [];
  } else {
    for (k in path_node) {
      if (path_node.hasOwnProperty(k)) {
        this[k] = path_node[k];
      }
    }

    if (loc && path_node.loc) {
      if (loc.end.line > path_node.loc.end.line || loc.end.column > path_node.loc.end.column) {
        this.loc.end = loc.end;
      }
    }
  }

  if (component) {
    c = [component.id];
    if (component.start !== null) {
      c.push(component.start);
    }
    if (component.end !== null) {
      c.push(component.end);
    }
    this.components.push(c);
  }
}

function FilterNode(filter, body, loc) {
  this.type = "Filter";
  this.body = [body];
  this.loc = loc;
}

function StringNode(str, loc) {
  this.type = "String";
  this.body = str;
  this.loc = loc;
}

function IfNode(body, loc) {
  this.type = "If";
  this.body = body;
  this.loc = loc;
}

function ConditionBranchNode(branch, exprs, body, loc) {
  this.type = "ConditionBranchNode";
  this.branch = branch;
  this.expressions = exprs;
  this.body = body;
  this.loc = loc;
}

function ExpressionNode(op, v1, v2, _negate, loc) {
  this.type = "Expression";
  this.operator = op;
  this.value1 = v1;
  this.value2 = v2;
  this.negate = _negate;
  this.relation = null; // relation gets set in "exprs" production
  this.loc = loc;
}

function FunctionNode(name, args, loc) {
  this.type = "Function";
  this.name = name;
  this.args = args;
  this.loc = loc;
}

function AliasNode(path, id, loc) {
  this.type = "Alias";
  this.path = path;
  this.id = id;
  this.loc = loc;
}

function UnaliasNode(id, loc) {
  this.type = "Unalias";
  this.id = id;
  this.loc = loc;
}

function IncludeNode(path, loc) {
  this.type = "Include";
  this.path = path;
  this.loc = loc;
}

function ForNode(id, path, body, loc) {
  this.type = "For";
  this.id = id;
  this.path = path;
  this.body = body;
  this.loc = loc;
}

function SwitchNode(control, cases, else_case, loc) {
  this.type = "Switch";
  this.control = control;
  this.cases = cases;
  this.else_case = else_case;
  this.loc = loc;
}

function WithNode(path, name, body, loc) {
  this.type = "With";
  this.path = path;
  this.name = name;
  this.body = body;
  this.loc = loc;
}

parser.ast = {};
parser.ast.Loc = Loc;
parser.ast.VoidTagNode = VoidTagNode;
parser.ast.CodeTagNode = CodeTagNode;
parser.ast.TagNode = TagNode;
parser.ast.TextNode = TextNode;
parser.ast.NameNode = NameNode;
parser.ast.IdNode = IdNode;
parser.ast.PathNode = PathNode;
parser.ast.FilterNode = FilterNode;
parser.ast.StringNode = StringNode;
parser.ast.IfNode = IfNode;
parser.ast.ConditionBranchNode = ConditionBranchNode;
parser.ast.ExpressionNode = ExpressionNode;
parser.ast.FunctionNode = FunctionNode;
parser.ast.AliasNode = AliasNode;
parser.ast.ForNode = ForNode;
parser.ast.SwitchNode = SwitchNode;
parser.ast.WithNode = WithNode;
/* generated by jison-lex 0.2.1 */
var lexer = (function(){
var lexer = {

EOF:1,

parseError:function parseError(str, hash) {
        if (this.yy.parser) {
            this.yy.parser.parseError(str, hash);
        } else {
            throw new Error(str);
        }
    },

// resets the lexer, sets new input
setInput:function (input) {
        this._input = input;
        this._more = this._backtrack = this.done = false;
        this.yylineno = this.yyleng = 0;
        this.yytext = this.matched = this.match = '';
        this.conditionStack = ['INITIAL'];
        this.yylloc = {
            first_line: 1,
            first_column: 0,
            last_line: 1,
            last_column: 0
        };
        if (this.options.ranges) {
            this.yylloc.range = [0,0];
        }
        this.offset = 0;
        return this;
    },

// consumes and returns one char from the input
input:function () {
        var ch = this._input[0];
        this.yytext += ch;
        this.yyleng++;
        this.offset++;
        this.match += ch;
        this.matched += ch;
        var lines = ch.match(/(?:\r\n?|\n).*/g);
        if (lines) {
            this.yylineno++;
            this.yylloc.last_line++;
        } else {
            this.yylloc.last_column++;
        }
        if (this.options.ranges) {
            this.yylloc.range[1]++;
        }

        this._input = this._input.slice(1);
        return ch;
    },

// unshifts one char (or a string) into the input
unput:function (ch) {
        var len = ch.length;
        var lines = ch.split(/(?:\r\n?|\n)/g);

        this._input = ch + this._input;
        this.yytext = this.yytext.substr(0, this.yytext.length - len - 1);
        //this.yyleng -= len;
        this.offset -= len;
        var oldLines = this.match.split(/(?:\r\n?|\n)/g);
        this.match = this.match.substr(0, this.match.length - 1);
        this.matched = this.matched.substr(0, this.matched.length - 1);

        if (lines.length - 1) {
            this.yylineno -= lines.length - 1;
        }
        var r = this.yylloc.range;

        this.yylloc = {
            first_line: this.yylloc.first_line,
            last_line: this.yylineno + 1,
            first_column: this.yylloc.first_column,
            last_column: lines ?
                (lines.length === oldLines.length ? this.yylloc.first_column : 0)
                 + oldLines[oldLines.length - lines.length].length - lines[0].length :
              this.yylloc.first_column - len
        };

        if (this.options.ranges) {
            this.yylloc.range = [r[0], r[0] + this.yyleng - len];
        }
        this.yyleng = this.yytext.length;
        return this;
    },

// When called from action, caches matched text and appends it on next action
more:function () {
        this._more = true;
        return this;
    },

// When called from action, signals the lexer that this rule fails to match the input, so the next matching rule (regex) should be tested instead.
reject:function () {
        if (this.options.backtrack_lexer) {
            this._backtrack = true;
        } else {
            return this.parseError('Lexical error on line ' + (this.yylineno + 1) + '. You can only invoke reject() in the lexer when the lexer is of the backtracking persuasion (options.backtrack_lexer = true).\n' + this.showPosition(), {
                text: "",
                token: null,
                line: this.yylineno
            });

        }
        return this;
    },

// retain first n characters of the match
less:function (n) {
        this.unput(this.match.slice(n));
    },

// displays already matched input, i.e. for error messages
pastInput:function () {
        var past = this.matched.substr(0, this.matched.length - this.match.length);
        return (past.length > 20 ? '...':'') + past.substr(-20).replace(/\n/g, "");
    },

// displays upcoming input, i.e. for error messages
upcomingInput:function () {
        var next = this.match;
        if (next.length < 20) {
            next += this._input.substr(0, 20-next.length);
        }
        return (next.substr(0,20) + (next.length > 20 ? '...' : '')).replace(/\n/g, "");
    },

// displays the character position where the lexing error occurred, i.e. for error messages
showPosition:function () {
        var pre = this.pastInput();
        var c = new Array(pre.length + 1).join("-");
        return pre + this.upcomingInput() + "\n" + c + "^";
    },

// test the lexed token: return FALSE when not a match, otherwise return token
test_match:function (match, indexed_rule) {
        var token,
            lines,
            backup;

        if (this.options.backtrack_lexer) {
            // save context
            backup = {
                yylineno: this.yylineno,
                yylloc: {
                    first_line: this.yylloc.first_line,
                    last_line: this.last_line,
                    first_column: this.yylloc.first_column,
                    last_column: this.yylloc.last_column
                },
                yytext: this.yytext,
                match: this.match,
                matches: this.matches,
                matched: this.matched,
                yyleng: this.yyleng,
                offset: this.offset,
                _more: this._more,
                _input: this._input,
                yy: this.yy,
                conditionStack: this.conditionStack.slice(0),
                done: this.done
            };
            if (this.options.ranges) {
                backup.yylloc.range = this.yylloc.range.slice(0);
            }
        }

        lines = match[0].match(/(?:\r\n?|\n).*/g);
        if (lines) {
            this.yylineno += lines.length;
        }
        this.yylloc = {
            first_line: this.yylloc.last_line,
            last_line: this.yylineno + 1,
            first_column: this.yylloc.last_column,
            last_column: lines ?
                         lines[lines.length - 1].length - lines[lines.length - 1].match(/\r?\n?/)[0].length :
                         this.yylloc.last_column + match[0].length
        };
        this.yytext += match[0];
        this.match += match[0];
        this.matches = match;
        this.yyleng = this.yytext.length;
        if (this.options.ranges) {
            this.yylloc.range = [this.offset, this.offset += this.yyleng];
        }
        this._more = false;
        this._backtrack = false;
        this._input = this._input.slice(match[0].length);
        this.matched += match[0];
        token = this.performAction.call(this, this.yy, this, indexed_rule, this.conditionStack[this.conditionStack.length - 1]);
        if (this.done && this._input) {
            this.done = false;
        }
        if (token) {
            return token;
        } else if (this._backtrack) {
            // recover context
            for (var k in backup) {
                this[k] = backup[k];
            }
            return false; // rule action called reject() implying the next rule should be tested instead.
        }
        return false;
    },

// return next match in input
next:function () {
        if (this.done) {
            return this.EOF;
        }
        if (!this._input) {
            this.done = true;
        }

        var token,
            match,
            tempMatch,
            index;
        if (!this._more) {
            this.yytext = '';
            this.match = '';
        }
        var rules = this._currentRules();
        for (var i = 0; i < rules.length; i++) {
            tempMatch = this._input.match(this.rules[rules[i]]);
            if (tempMatch && (!match || tempMatch[0].length > match[0].length)) {
                match = tempMatch;
                index = i;
                if (this.options.backtrack_lexer) {
                    token = this.test_match(tempMatch, rules[i]);
                    if (token !== false) {
                        return token;
                    } else if (this._backtrack) {
                        match = false;
                        continue; // rule action called reject() implying a rule MISmatch.
                    } else {
                        // else: this is a lexer rule which consumes input without producing a token (e.g. whitespace)
                        return false;
                    }
                } else if (!this.options.flex) {
                    break;
                }
            }
        }
        if (match) {
            token = this.test_match(match, rules[index]);
            if (token !== false) {
                return token;
            }
            // else: this is a lexer rule which consumes input without producing a token (e.g. whitespace)
            return false;
        }
        if (this._input === "") {
            return this.EOF;
        } else {
            return this.parseError('Lexical error on line ' + (this.yylineno + 1) + '. Unrecognized text.\n' + this.showPosition(), {
                text: "",
                token: null,
                line: this.yylineno
            });
        }
    },

// return next match that has a token
lex:function lex() {
        var r = this.next();
        if (r) {
            return r;
        } else {
            return this.lex();
        }
    },

// activates a new lexer condition state (pushes the new lexer condition state onto the condition stack)
begin:function begin(condition) {
        this.conditionStack.push(condition);
    },

// pop the previously active lexer condition state off the condition stack
popState:function popState() {
        var n = this.conditionStack.length - 1;
        if (n > 0) {
            return this.conditionStack.pop();
        } else {
            return this.conditionStack[0];
        }
    },

// produce the lexer rule set which is active for the currently active lexer condition state
_currentRules:function _currentRules() {
        if (this.conditionStack.length && this.conditionStack[this.conditionStack.length - 1]) {
            return this.conditions[this.conditionStack[this.conditionStack.length - 1]].rules;
        } else {
            return this.conditions["INITIAL"].rules;
        }
    },

// return the currently active lexer condition state; when an index argument is provided it produces the N-th previous condition state, if available
topState:function topState(n) {
        n = this.conditionStack.length - 1 - Math.abs(n || 0);
        if (n >= 0) {
            return this.conditionStack[n];
        } else {
            return "INITIAL";
        }
    },

// alias for begin(condition)
pushState:function pushState(condition) {
        this.begin(condition);
    },

// return the number of states currently on the stack
stateStackSize:function stateStackSize() {
        return this.conditionStack.length;
    },
options: {},
performAction: function anonymous(yy,yy_,$avoiding_name_collisions,YY_START
/**/) {

var YYSTATE=YY_START;
switch($avoiding_name_collisions) {
case 0:
  var tokens;

  this.popState();

  tokens = [];
  while (0 < indent_stack[0]) {
    tokens.unshift("DEDENT");
    indent_stack.shift();
  }

  if (tokens.length) {
    tokens.push("EOF");
    return tokens;
  } else {
    return "EOF";
  }

break;
case 1:// ignore blank lines
break;
case 2:
  // Set level to length of string minus newline character.
  var level = yy_.yytext.length - 1;

  this.popState();

  if (level > indent_stack[0]) {
    indent_stack.unshift(level);
    return "INDENT";
  }

  if (level < indent_stack[0]) {
    var tokens = [];
    while (level < indent_stack[0]) {
      tokens.unshift("DEDENT");
      indent_stack.shift();
    }
 
    if (tokens.length) {
      return tokens;
    }
  }

break;
case 3:
  var level = yy_.yytext.length;

  if (level > indent_stack[0]) {
    indent_stack.unshift(level);
    return "INDENT";
  }

break;
case 4:// ignore comments
break;
case 5:// ignore comments
break;
case 6:return "STR";
break;
case 7:return "ISTR";
break;
case 8:this.pushState("expr"); return yy_.yytext.toUpperCase();
break;
case 9:return yy_.yytext.toUpperCase();
break;
case 10:return "OP";
break;
case 11:return "BOOL";
break;
case 12:return "TYPE";
break;
case 13:this.popState();
break;
case 14:this.pushState("code"); return "CODETAG";
break;
case 15:this.pushState("tag"); return "VOIDTAG";
break;
case 16:this.pushState("tag"); return "TAG";
break;
case 17:yy_.yytext = yy_.yytext.slice(1); return "TAGID";
break;
case 18:yy_.yytext = yy_.yytext.slice(1); return "TAGCLASS";
break;
case 19:yy_.yytext = yy_.yytext.replace(/^->\s*/, ''); return "TAGSRC";
break;
case 20:// ignore whitespace
break;
case 21:this.pushState("attrs"); return yy_.yytext;
break;
case 22:this.popState(); return yy_.yytext;
break;
case 23:return "ATTR";
break;
case 24:return "DATAATTR";
break;
case 25:return yy_.yytext;
break;
case 26:// ignore
break;
case 27:// ignore
break;
case 28:this.popState(); return "TEXT";
break;
case 29:
  this.popState();

  var tokens = [];
  while (code_level >= indent_stack[0]) {
    tokens.unshift("DEDENT");
    indent_stack.shift();
  }

  code_level = 0;

  tokens.shift();
  tokens.push("EOF");

  return tokens;

break;
case 30:yy_.yytext = ''; return "BLANKLINE";
break;
case 31:
  // Set level to length of string minus newline character.
  var indent_level = yy_.yytext.length - 1; 

  if (code_level === 0) {
    code_level = indent_level;
    indent_stack.unshift(indent_level);
    yy_.yytext = yy_.yytext.slice(code_level + 1);
    return "INDENT";

  } else if (indent_level < code_level) {
    code_level = 0;

    var tokens = [];
    while (indent_level < indent_stack[0]) {
      tokens.unshift("DEDENT");
      indent_stack.shift();
    }

    this.popState();
    return tokens;

  } else if (code_level > 0) {
    yy_.yytext = yy_.yytext.slice(code_level + 1);
    return "INDENT";
  }

break;
case 32:return "LINE";
break;
case 33:this.pushState("name"); return "NSTART";
break;
case 34:this.popState(); return "NSTOP";
break;
case 35:return "ID";
break;
case 36:return yy_.yytext;
break;
case 37:return "INT";
break;
case 38:return "COMMA";
break;
case 39:
  this.pushState("fn"); 
  yy_.yytext = yy_.yytext.slice(1, -1); 
  return "FN";

break;
case 40:this.popState(); return "RPAREN";
break;
case 41:return "COMMA";
break;
case 42:return "EQ";
break;
case 43:/* ignore */
break;
case 44:/* ignore */
break;
case 45:
  this.pushState("filter"); 
  yy_.yytext = yy_.yytext.slice(1); 
  return "FILTER";

break;
case 46:
  this.popState();

  var tokens = [];
  while (filter_level >= indent_stack[0]) {
    tokens.unshift("DEDENT");
    indent_stack.shift();
  }

  filter_level = 0;

  tokens.shift();
  tokens.push("EOF");

  return tokens;

break;
case 47:
  var level, tokens;

  level = yy_.yytext.length - 1;

  /* Set lexer into Filter Mode. */
  if (filter_level === 0) {
    filter_level = level;
    indent_stack.unshift(level);
    return "INDENT";

  } 
  /* Set lexer back into Standard Mode. */
  else if (level < filter_level) {
    this.popState();
    filter_level = 0;

    tokens = [];
    while (indent_stack[0] > level) {
      tokens.push("DEDENT");
      indent_stack.shift();
    }

    return tokens;
  }
  /* Convert new line to a space for output. */
  else {
    yy_.yytext = ' ';
    return "TEXT";
  }

break;
case 48:return "BREAK";
break;
case 49:return "CONTINUE";
break;
case 50:// ignore all other whitespace
break;
case 51:this.pushState("raw"); return "RAW";
break;
case 52:return "TEXT";
break;
}
},
rules: [/^(?:([ \t])*$)/,/^(?:[\n\r]+([ \t])*(?![^\n\r]))/,/^(?:[\n\r]([ \t])*)/,/^(?:^([ \t])+)/,/^(?:[\n\r]*([ \t])*"""(.|\n)*?""")/,/^(?:[\n\r]*([ \t])*\/\/[^\n]*)/,/^(?:(("(\\"|[^'"']|')*")|('(\\'|[^"'"]|")*')))/,/^(?:i(("(\\"|[^'"']|')*")|('(\\'|[^"'"]|")*')))/,/^(?:((for|if|elif|else|alias|unalias|with|include|case|when))(?=([ \t])))/,/^(?:((exists|not|and|or|in|is|as))(?=([ \t])))/,/^(?:((==|!=|>=|<=|>|<|%)))/,/^(?:true|false\b)/,/^(?:((List|Hash|String|Boolean|Integer|Number)))/,/^(?:(?=\n))/,/^(?:pre|code\b)/,/^(?:((area|base|br|col|embed|hr|img|input|keygen|link|menuitem|meta|param|source|track|wbr)))/,/^(?:([a-zA-Z][a-zA-Z0-9]*))/,/^(?:(#[a-zA-Z][a-zA-Z0-9\-\:_]*))/,/^(?:(\.-?[_a-zA-Z]+[_a-zA-Z0-9\-]*))/,/^(?:->([ \t])*(("(\\"|[^'"']|')*")|('(\\'|[^"'"]|")*')))/,/^(?:([ \t])+)/,/^(?:\()/,/^(?:\))/,/^(?:[a-zA-Z][a-zA-Z0-9\-_]*)/,/^(?:\*[a-zA-Z][a-zA-Z0-9\-]*)/,/^(?:=)/,/^(?:\n)/,/^(?:,)/,/^(?:([^"@"\n]+))/,/^(?:\s*$)/,/^(?:[\n\r]+([ \t])*(?![^\n\r]))/,/^(?:[\n\r]([ \t])*)/,/^(?:.*)/,/^(?:@\{)/,/^(?:\})/,/^(?:(\$?[a-zA-Z_][a-zA-Z0-9_]{0,254}))/,/^(?:\.|\[|\]|:)/,/^(?:(\+|-)?(0|[1-9][0-9]*))/,/^(?:,)/,/^(?:@(\$?[a-zA-Z_][a-zA-Z0-9_]{0,254})\()/,/^(?:\))/,/^(?:,)/,/^(?:=)/,/^(?:\s)/,/^(?:\n)/,/^(?::(\$?[a-zA-Z_][a-zA-Z0-9_]{0,254}))/,/^(?:\s*$)/,/^(?:[\n\r]([ \t])*)/,/^(?:!break\b)/,/^(?:!continue\b)/,/^(?:([ \t])+)/,/^(?:~)/,/^(?:([^"@"\n]+))/],
conditions: {"tag":{"rules":[0,1,2,17,18,19,20,21,28,33,39,50],"inclusive":true},"attrs":{"rules":[6,7,22,23,24,25,26,27,33,50],"inclusive":true},"name":{"rules":[6,7,11,33,34,35,36,37,38,39,50],"inclusive":true},"filter":{"rules":[1,33,39,46,47,50,52],"inclusive":true},"raw":{"rules":[1,2,33,39,50,52],"inclusive":true},"expr":{"rules":[6,7,9,10,11,12,13,33,35,36,37,39,50],"inclusive":true},"fn":{"rules":[6,7,11,33,35,36,37,40,41,42,43,44,50],"inclusive":true},"code":{"rules":[29,30,31,32,33,50],"inclusive":true},"INITIAL":{"rules":[0,1,2,3,4,5,8,14,15,16,33,39,45,48,49,50,51,52],"inclusive":true}}
};
indent_stack = [0];
filter_level = 0;
code_level = 0;;
return lexer;
})();
parser.lexer = lexer;
function Parser () {
  this.yy = {};
}
Parser.prototype = parser;parser.Parser = Parser;
return new Parser;
})();


if (typeof require !== 'undefined' && typeof exports !== 'undefined') {
exports.parser = manana_parser;
exports.Parser = manana_parser.Parser;
exports.parse = function () { return manana_parser.parse.apply(manana_parser, arguments); };
exports.main = function commonjsMain(args) {
    if (!args[1]) {
        console.log('Usage: '+args[0]+' FILE');
        process.exit(1);
    }
    var source = require('fs').readFileSync(require('path').normalize(args[1]), "utf8");
    return exports.parser.parse(source);
};
if (typeof module !== 'undefined' && require.main === module) {
  exports.main(process.argv.slice(1));
}
}/* ******************************************************
 * Author: Chad Angelelli <chad@angelel.li>
 * Contributors/Special Thanks:
 *    Ray Harris <ray@harris.net>
 *    Mason Armour <mason@myshorttrack.com>
 *    Chris Hacker <chris@myshorttrack.com>
 *    Brigette LeVert <brigette.levert@gmail.com>
 * ******************************************************/

/**
 * The Mañana Templating Language
 * @version 1.0
 * @exports Manana
 */
(function(exports) {
  var _manana_is_server_side, _manana_is_client_side;

  // _____________________________________________ Configuration
  _manana_is_server_side =  typeof require        !== 'undefined' && 
                            typeof module         !== 'undefined' && 
                            typeof module.exports !== 'undefined' ;

  _manana_is_client_side = !_manana_is_server_side;


 
  // _____________________________________________ Mañana
  /**
   * Manana
   * @class Manana
   * @param {string} [view_dir] - View Directory if using server-side
   */
  function Manana(view_dir) {
    var self = this;

    // ........................................... 
    /**
     * @memberof Manana
     * @property {String} name - View name
     * @property {String} template - Raw Mañana template
     * @property {Array} ir - Mañana intermediate representation
     * @property {String} [result=''] - Final result from evaluating all nodes
     * @property {Mixed} [context={}] - Context passed to interpreter methods
     * @property {MananaView} view - Current MananaView object
     * @property {Array} views - Array of MananaView objects
     * @property {Number} [view_level=0] - View level, increments when "include" is called .
     * @property {Array} ancestry - Array of MananaView objects, added to when "include" is called.
     * @property {MananaError} err - Last error thrown from interpreter
     * @property {Boolean} _is_server_side - Flag
     * @property {Boolean} _is_client_side - Flag
     * @property {Boolean} [_in_loop=false] - Flag to tell interpreter if in Loop State
     * @property {Boolean} [_break_loop=false] - Flag to tell interpreter to break loop
     * @property {Boolean} [_continue_loop=false] - Flag to tell interpreter to continue loop
     * @property {Boolean} [_silence_error_logging=false] - Flag to tell interpreter to log errors or not
     * @property {Boolean} [_format_result=false] - Flag to tell interpreter to format result text
     */ 

    // . .. ... .. . .. ... .. . .. ... .. . .. ... .. . .. ... .. .
    this.name       = '';
    this.template   = '';
    this.ir         = '';
    this.result     = '';
    this.context    = {};
    this.view       = {}; // the current view object
    this.view_level = 0;
    this.ancestry   = [];
    this.err        = null;

    // . .. ... .. . .. ... .. . .. ... .. . .. ... .. . .. ... .. .
    this._is_server_side        = _manana_is_server_side;
    this._is_client_side        = !_manana_is_server_side;
    this._in_loop               = false;
    this._break_loop            = false;
    this._continue_loop         = false;
    this._silence_error_logging = false;
    this._format_result         = false;

    // . .. ... .. . .. ... .. . .. ... .. . .. ... .. . .. ... .. .
    this.format = {}; // options for output format

    // . .. ... .. . .. ... .. . .. ... .. . .. ... .. . .. ... .. .
    /**
     * Low-level methods for converting AST nodes to output
     * @memberof Manana
     * @type {object}
     * @namespace Manana.interpreter
     */
    this.interpreter = {}; 

    /**
     * Mañana's Namespaces
     * @memberof Manana
     * @type {object}
     * @namespace Manana.namespace
     */
    this.namespace = {}; 

    /**
     * A cache of all known views
     * @memberof Manana
     * @type {object}
     * @namespace Manana.views
     */
    this.views = {}; 

    /**
     * Text Manipulation Methods
     * @memberof Manana
     * @type {object}
     * @namespace Manana.text
     */
    this.text = {}; 

    /**
     * Marshaling methods for transporting views/contexts
     * @memberof Manana
     * @type {object}
     * @namespace Manana.marshal
     */
    this.marshal = {}; 

    /**
     * Mañana Functions. Built-in or user-defined.
     * @memberof Manana
     * @type {object}
     * @namespace Manana.fns 
     */
    this.fns = {}; 

    /**
     * Mañana Raw Functions. Built-in or user-defined.
     * @memberof Manana
     * @type {object}
     * @namespace Manana.raw_fns 
     */
    this.raw_fns = {}; // Raw functions. Built-in or user-defined.

    /**
     * Validation methods
     * @memberof Manana
     * @type {object}
     * @namespace Manana.validate
     */
    this.validate    = {}; // Validation methods.

    // ........................................... 
    /**
     * A Mañana Namespace
     * @class MananaNamespace
     * @param {string} name - The name of the Namespace
     * @param {*} data - The data that is available to the view
     * @param {MananaNamespace|null} $parent - The parent Namespace
     */
    function MananaNamespace(name, data, $parent) {
      this.type = 'MananaNamespace';
      this.name = name;
      this.data = data;
      this.$parent = $parent;
    } // end MananaNamespace()
 
    // ........................................... 
    /**
     * A Mañana View Object
     * @class MananaView
     * @param {Object} args - The paramaters to set up a MananaView
     * @param {string} args.name - The name of the view
     * @param {string} args.template - The source code for the template
     * @param {Mixed} args.context - The context for the MananaView
     * @param {MananaView} args.$parent - The ancestral parent MananaView
     */
    function MananaView(args) {
      this.name     = args.name;
      this.template = args.template;
      this.context  = args.context;
      this.$level   = args.$level; 
      this.$parent  = args.$parent;
    } // end MananaView()
 
    // ........................................... 
    /**
     * A Mañana Error
     * @class MananaError
     * @param {string} message - The error message
     * @param {Object} [loc] - A Manana.ast.Loc node
     */
    function MananaError(message, loc) {
      this.name = "MananaError";
      this.message = message;
      this.loc = loc;

      if (!self._silence_error_logging) {
        console.log('MananaError');
        console.log('\ttemplate: ', self.view.name);
        console.log('\tmessage: ', message);

        if (is(loc, 'undefined')) {
          console.log('\t-- no line/column information provided');
        } else {
          console.log('\tline: ', loc.start.line);
          console.log('\tcolumn: ', loc.start.column, '-', loc.end.column);
        }

        console.log('\tview object: ', self.view);
      }
    } // end MananaError()

    // ...........................................  Validation shorthand and helpers
    function is(v, t)  { return typeof v === t; }
    function isNull(v) { return v === null; }
    function isBool(v) { return is(v, "boolean"); }
    function isStr(v)  { return is(v, "string"); }
    function isNum(v)  { return is(v, "number"); }
    function isInt(v)  { return is(v, "number") && parseFloat(v) == parseInt(v, 10) && !isNaN(v); }
    function isArr(v)  { return Object.prototype.toString.call(v) === '[object Array]'; }
    function isObj(v)  { return Object.prototype.toString.call(v) === '[object Object]'; }

    function silence() {
      self._silence_error_logging = true;
    }
 
    function unsilence() {
      self._silence_error_logging = false;
    }

    function setUndefined() {
      return;
    }
 
    function strFmt(s, o) {
      return s.replace(/{([^{}]*)}/g, function(a, b) { 
        var r = o[b]; 
        return isStr(r) || isNum(r) ? r : a; 
      });
    };
 
    function repeatStr(s, n) {
      return new Array(n + 1).join(s);
    };
 
    function objectSize(obj) {
      var size = 0, key;
      for (key in obj) {
        if (obj.hasOwnProperty(key)) {
          size++;
        }
      }
      return size;
    };

    function jd(label, x) {
      console.log("\n\n" + label + "\n==========================================>\n>>>");
      console.log(JSON.stringify(x, null, 4));
      console.log("\n\n<<< END " + label + "\n\n");
    }

    // ........................................... 
    if (this._is_server_side) {
      if (typeof manana_parser !== 'undefined') {
        this.parser = manana_parser;
        this.Parser = manana_parser.Parser;
      } else {
        this.Parser = require('./manana_parser');
        this.parser = this.Parser.parser;
      }

      this.file_system = require('fs');

      this.__dirname = require('path').dirname(require.main.filename);

      if (is(view_dir, "undefined")) {
        this.view_dir = this.__dirname; 
      } else { 
        this.view_dir = view_dir;
      }

      if (this.view_dir[this.view_dir.length - 1] == '/') {
        this.view_dir = this.view_dir.slice(0, -1);
      }
    } else {
      this.parser = manana_parser;
      this.Parser = manana_parser.Parser;
    }

    // ...........................................  
    /**
     * Read a template from disk, or script from DOM
     * @memberof Manana
     * @method getTemplate
     * @param {string} name - The name of the template
     */
    this.getTemplate = function(name) {
      var template, abs_name, scripts, i, l, s, s_name;

      if (self._is_server_side) {
        try {
          if (name[0] == '.') {
            abs_name = self.__dirname + '/' + name.slice(2);
          } else if (name[0] == '/') {
            abs_name = name;
          } else {
            abs_name = self.view_dir + '/' + name; 
          }

          if (!/\.manana$/.test(abs_name)) {
            abs_name += ".manana";
          }

          template = self.file_system.readFileSync(abs_name, 'utf-8');

        } catch (e) {
          self.err = new MananaError(strFmt("Invalid name '{p}' provided to getTemplate function", {p:name}));
          throw self.err;
        }
      } else { // self._is_client_side
        scripts = document.getElementsByTagName("script"); 
        for (i = 0, l = scripts.length; i < l; i++) {
          s = scripts[i];
          s_name = s.getAttribute("data-view-name");
          if (s_name == name) {
            template = s.innerHTML;
          }
        }
      }

      if (!template.length) {
        self.err = new MananaError(strFmt("Template '{n}' has no content.", {n:name}));
        throw self.err
      }

      return template;
    }; // end Manana.getTemplate()

    // ...........................................  
    /**
     * Render a template
     * @memberof Manana
     * @method render
     * @param {Object} [setup=null] - A single object containing  setup paramaters
     * @param {String} setup.view - The name of the view 
     * @param {*} [setup.context={}] - The context 
     * @param {Object} [setup.format={}] - The format options 
     * @param {string} [name=null] - The name of the template
     * @param {*} [context={}] - A non-falsy value to be passed into the template
     * @param {Object} [options={}] - Optional options for rendering
     */
    this.render = function() {
      var err_data, args, name, context, options, form, i, r, level;

      // Configure error data.
      err_data = {"arguments": arguments};

      // Render method can accept a single object as its only argument,
      // or (name [, context [, options]]) as positional arguments.
      if (isObj(arguments[0])) {
        if (arguments.length > 1) {
          self.err = new MananaError("Too many arguments in Render function", err_data);
          throw self.err;
        }
        args = arguments[0];
        name = args.view;
        context = args.context || {};
        options = args.options || {};
      }
      else if (isStr(arguments[0])) {
        name = arguments[0];
        context = arguments[1] || {};
        options = arguments[2] || {};
      }

      // Validate required config is present.
      self.name = name;
      self.template = self.getTemplate(self.name);
      
      try {
        self.ir = self.parser.parse(self.template);
      } catch (e) {
        self.err = new MananaError(e.message);
        console.log('\n=============================================\n');
        console.log('--- Manana Parse Error: ', self.name);
        console.log('--- Template:\n');
        console.log(self.template);
        console.log('\n=============================================\n');
        throw self.err;
      }

      // Setup default namespace "root"
      self.namespace.root = new MananaNamespace('root', context, null);
      self.context = self.namespace.root;

      // Configure formatting options for output.
      if ('format' in options) {
        self._format_result = true;

        self.format = {};
        self.format.indent          = parseInt(options.format.indent) || 4;
        self.format.indent_char     = options.format.indent_char || " ";
        self.format.indent_str      = repeatStr(self.format.indent_char, self.format.indent);
        self.format.max_line_length = options.format.max_line_length || 72;
      }
      else {
        self._format_result = false;
      }

      // Setup View
      self.views[name] = new MananaView({
        name: name,
        template: self.template,
        context: self.context,
        $level: 0,
        $parent: null
      });

      self.view = self.views[name];
      self.ancestry = [self.view];

      // Setup window
      self['$window'];
      if (typeof window !== "undefined") {
        self['$window'] = window;
      }

      // Get result.
      self.result = '';

      i = level = 0;
      while (form = self.ir[i]) {
        r = self.interpreter.evalForm(form, self.context, level);
        self.result += isObj(r) ? JSON.stringify(r) : r;
        i++;
      }

      if (options.encode) {
        self.result = self.text.encode(self.result);
      }

      return self.result;
    }; // end Manana.render()

    // _____________________________________________ Interpreter
    
    // ...........................................  
    /**
     * Low-level method to evaluate a Mañana AST Node
     * @memberof Manana.interpreter
     * @method evalForm
     * @param {Object} form - A Mañana AST node 
     * @param {*} context - A value to be passed as the context for a view
     */
    this.interpreter.evalForm = function(form, context, level) {
      var res, i, _is_node, _is_special;

      if (isObj(form) && 'type' in form) {
        _is_node = true;

        switch (form.type) {
          case 'Path':
          case 'Function':
            _is_special = true; 
            break;
          default:
            _is_special = false;
        }
      }
      else {
        _is_node = false;
      }

      if (_is_special) {
        res = self.interpreter[form.type](form, context, level);
      }
      else if (_is_node) {
        res = '' + self.interpreter[form.type](form, context, level);
      }
      else if (isArr(form)) {
        res = '';
        i = 0;
        while (!is(form[i], "undefined")) {
          res += self.interpreter.evalForm(form[i], context, level);
          i++;
        }
      } 
      else {
        res = form;
      }

      return res;
    }; // end Manana.interpreter.evalForm()

    // ...........................................  
    /**
     * Include a sub-template into current, using same context
     * @memberof Manana.interpreter
     * @method Include
     * @param {Object} form - A Mañana.ast.IncludeNode 
     * @param {*} context - A value to be passed as the context for a view
     * @param {Number} level - The current level used for output formatting
     */
    this.interpreter.Include = function(form, context, level) {
      var name, template, ir, $parent, i, form, res;

      name = self.interpreter.evalForm(form.path, context, level);
      template = self.getTemplate(name);

      try {
        ir = self.parser.parse(template);
      } catch (e) {
        self.err = new MananaError(e.message);
        console.log('--- Manana Parse Error: ', name);
        console.log('--- Template (Include)');
        console.log(template);
        throw self.err;
      }

      $parent = self.view;
      self.view_level++;

      self.views[name] = new MananaView({
        name: name,
        template: template,
        context: context,
        $level: self.view_level,
        $parent: $parent 
      });

      self.view = self.views[name];

      if (self.view_level < self.ancestry.length) {
        self.ancestry = self.ancestry.slice(0, self.view_level);
      }

      self.ancestry.push(self.view);

      i = 0;
      res = '';
      while (form = ir[i]) {
        res += self.interpreter.evalForm(form, context, level);
        i++;
      }

      self.view = $parent;
      self.view_level--;

      return res;
    }; // end Manana.interpreter.Include()

    // ...........................................  
    /**
     * Resolve a path for an expression, argument, or name
     * @memberof Manana.interpreter
     * @method Path
     * @param {Object} form - A Mañana.ast.PathNode 
     * @param {*} context - A value to be passed as the context for a view
     * @param {Number} level - The current level used for output formatting
     */
    this.interpreter.Path = function(form, context, level) {
      var node, components, target, i, index, slice, traceback, meth, tmp_node, _node_set;
      var traceback_str, index_str, slice_str;

      node = context;
      components = JSON.parse(JSON.stringify(form.components));
      traceback = [];

      if (components[0][0] == '$manana') {
        node = self;
        traceback.push(components[0][0]);
        components.shift();

      } else if (components[0][0] == '$window') {
        if (typeof window !== 'undefined') {
          node = window;
          traceback.push(components[0][0]);
          components.shift();
        } else {
          self.err = new MananaError("Invalid path: $window is not defined", form.loc);
          throw self.err;
        }
      }

      i = 0;
      while (!is(components[i], "undefined")) {
        target = self.interpreter.evalForm(components[i][0], context, level);
        index  = self.interpreter.evalForm(components[i][1], context, level);
        slice  = self.interpreter.evalForm(components[i][2], context, level);

        index_str = (index === '*' ? '' : index);
        slice_str = (slice === '*' ? '' : slice);

        if (!is(index, 'undefined')) {
          if (!is(slice, 'undefined')) {
            traceback_str = target + '[' + index_str + ':' + slice_str + ']';
          } else {
            traceback_str = target + '[' + index_str + ']';
          }
        } else if (!is(slice, 'undefined')) {
          traceback_str = target + '[:' + slice_str + ']';
        } else {
          traceback_str = target;
        }

        traceback.push(traceback_str);

        //................ 
        if (self.validate.isNamespace(node)) {
          if (target[0] == '$' && !is(node[target], 'undefined')) {
            node = node[target];

          } else if (!isNull(node.data) && !is(node.data[target], 'undefined')) {
            node = node.data[target];

          } else if (node.name == target) {
            node = node.data;

          } else if (!is(self.namespace[target], 'undefined')) {
            node = self.namespace[target];

          } else if (node.$parent) {
            tmp_node = node;
            while (tmp_node.$parent) {
              tmp_node = tmp_node.$parent;
              if (!is(tmp_node.data[target], 'undefined')) {
                node = tmp_node.data[target];
                _node_set = true;
                break;
              }
            }

            if (!_node_set) {
              self.err = new MananaError('Could not find path in ancestry: "' + traceback.join('.') + '"', form.loc);
              throw self.err;
            }

          } else {
            self.err = new MananaError('Invalid path in namespace: "' + traceback.join('.') + '"', form.loc);
            throw self.err;
          }

        } else if (!is(node, 'undefined')) { 
          if (!is(node[target], "undefined")) {
            node = node[target];
          } else {
            self.err = new MananaError('Invalid path: "' + traceback.join('.') + '"', form.loc);
            throw self.err;
          }

        } else {
          self.err = new MananaError("Undefined path: " + traceback.join('.'), form.loc);
          throw self.err;
        }

        //................ 
        if (!is(slice, 'undefined')) {
          if (isStr(node)) {
            node = node.split('');
          } else if (!isArr(node)) {
            self.err = new MananaError('slicing attempted on non-list: ' + traceback.join('.'), form.loc);
            throw self.err;
          }

          index = index === '*' ? 0 : parseInt(index);
          slice = slice === '*' ? node.length : parseInt(slice);
          node = node.slice(index, slice);

        } else if (!is(index, 'undefined')) {
          if (isObj(node) && !is(node[index], 'undefined')) {
            node = node[index];
          } else {
            index = index === '*' ? 0 : parseInt(index);
            if (index < 0) {
              index = node.length + index;
            }
            node = node[index];
          }
        }

        i++;
      } // end while

      //................ 
      if (is(node, 'undefined')) {
        self.err = new MananaError("Can't find path: " + traceback.join('.'), form.loc);
        throw self.err;
      }

      //................ 
      return node;
    }; // end Manana.interprteter.Path()

    // ...........................................  
    /**
     * Resolve a path for an expression, argument, or name
     * @memberof Manana.interpreter
     * @method Name
     * @param {Object} form - A Mañana.ast.NameNode 
     * @param {*} context - A value to be passed as the context for a view
     * @param {Number} level - The current level used for output formatting
     */
    this.interpreter.Name = function(form, context, level) {
      var res;

      if ('default_value' in form && !isNull(form.default_value)) {
        try {
          silence();
          res = self.interpreter.evalForm(form.path, context, level);
          unsilence();
        } catch (e) {
          unsilence();
          res = self.interpreter.evalForm(form.default_value, context, level);
        }
      } else {
        res = self.interpreter.evalForm(form.path, context, level);
      }

      return res;
    }; // end Manana.interpreter.Name()

    // ........................................... 
    /**
     * Create a temporary MananaNamespace to use for the duration of a block
     * @memberof Manana.interpreter
     * @method With
     * @param {Object} form - A Mañana.ast.WithNode 
     * @param {*} context - A value to be passed as the context for a view
     * @param {Number} level - The current level used for output formatting
     */
    this.interpreter.With = function(form, context, level) {
      var name, data, $parent, i, res;

      name = form.name !== null
        ? form.name
        : ('__with__' + Math.floor(Math.random() * (9999999 - 1000000) + 1000000));

      data = self.interpreter.evalForm(form.path, context, level);

      $parent = self.context;
      self.namespace[name] = new MananaNamespace(name, data, $parent);
      self.context = self.namespace[name];

      if (!isNull(form.body)) {
        i = 0; 
        res = '';
        while (!is(form.body[i], "undefined")) {
          res += self.interpreter.evalForm(form.body[i], self.context, level);
          i++;
        }
      }

      delete self.namespace[name];

      self.context = $parent;

      return res;
    }; // end Manana.interpreter.With()

    // ...........................................  
    /**
     * Create a MananaNamespace that persists
     * @memberof Manana.interpreter
     * @method Alias
     * @param {Object} form - A Mañana.ast.AliasNode 
     * @param {*} context - A value to be passed as the context for a view
     * @param {Number} level - The current level used for output formatting
     */
    this.interpreter.Alias = function(form, context, level) {
      var name, data;

      if (!self.validate.isNamespace(context)) {
        self.err = new MananaError("Invalid context passed to Alias method. Must be a valid namespace.", form.loc);
        throw self.err;
      }

      if (!is(context[name], "undefined")) {
        self.err = new MananaError(strFmt("Can't alias '{id}'. Name already taken in current context.", form.loc));
        throw self.err;
      }

      name = form.id;
      data = self.interpreter.evalForm(form.path, context, level);

      self.namespace[name] = data;

      return '';
    }; // end Manana.interpreter.Alias()

    // ...........................................  
    /**
     * Remove a MananaNamespace created by Alias
     * @memberof Manana.interpreter
     * @method Unalias
     * @param {Object} form - A Mañana.ast.UnaliasNode 
     * @param {*} context - A value to be passed as the context for a view
     * @param {Number} level - The current level used for output formatting
     */
    this.interpreter.Unalias = function(form, context, level) {
      var id;

      id = self.interpreter.evalForm(form.id, context, level);

      if (is(self.namespace[id], 'undefined')) {
        self.err = new MananaError(strFmt('Unknown alias "{id}". Can not unalias.', form.loc));
        throw self.err;
      }

      delete self.namespace[id];

      return '';
    }; // end Manana.interpreter.Unalias()

    // ...........................................  
    /**
     * Conditional operator used to evaluate true or false
     * @memberof Manana.interpreter
     * @method If
     * @param {Object} form - A Mañana.ast.IfNode 
     * @param {*} context - A value to be passed as the context for a view
     * @param {Number} level - The current level used for output formatting
     */
    this.interpreter.If = function(form, context, level) {
      var body,         // form.body
          num_branches, // number of branches (if, elif, else)
          branch,       // an inidivual branch
          last_branch,  // the last branch in the node tree - used to check for else
          branch_stop,  // index for when the top level while-loop should stop
          num_exprs,    // number of expressions to eval
          exprs,        // local reference to the expressions list 
          expr,         // an individual expression
          next_expr,    // the next expression in the list
          i,            // current index for branches
          j,            // current index for expressions
          op,           // operator for expression
          rel,          // relation for expression (null, and, or)
          v1,           // 1st value in expression
          v2,           // 2nd value in expression (optional)
          res,          // boolean result of expression evaluation
          out_body,     // forms to evaluate for output
          out,          // output string
          prev,         // boolean result of previous expression evaluation
          ctx,          // shorthand for ctx
          el,           // reference to each body element to evaluate
          _eval,        // local reference to self.interpreter.evalForm
          _has_else,    // flag: does an else-clause exist?
          _negate;      // flag: are we negating the boolean result?

      _eval        = self.interpreter.evalForm;
      ctx          = context;
      body         = form.body;
      num_branches = body.length;
      last_branch  = body[num_branches-1];
      _has_else    = last_branch.branch === 'else';
      branch_stop  = _has_else ? (num_branches - 1) : num_branches;

      i = 0;
      while (i < branch_stop) {
        res       = setUndefined();
        branch    = body[i];
        op        = branch.op;
        exprs     = branch.expressions;
        expr_stop = exprs.length;

        j = 0;
        while (j < expr_stop) {
          prev      = res;
          res       = setUndefined();
          expr      = exprs[j];
          next_expr = exprs[j+1] || false;
          op        = expr.operator;
          rel       = expr.relation;
          v1        = expr.value1;
          v2        = expr.value2;
          _negate   = expr.negate;

          switch (op) {
            //. .. ... ..  . .. ... .. . .. ... .. . .. ... .. . .. ... .. .
            case 'exists':
              silence();
              try {
                _eval(v1, ctx, level);
                res = true;
              } 
              catch (e) {
                res = false;
              } 
              finally {
                unsilence();
              }
              break;
           
            //. .. ... ..  . .. ... .. . .. ... .. . .. ... .. . .. ... .. .
            case 'in':
              v1 = _eval(v1, ctx, level);
              v2 = _eval(v2, ctx, level);
              if (isArr(v2) || isStr(v2))
                res = v2.indexOf(v1) > -1;
              else if (isObj(v2))
                res = (v1 in v2);
              break;
           
            //. .. ... ..  . .. ... .. . .. ... .. . .. ... .. . .. ... .. .
            case 'is':
              v1 = _eval(v1, ctx, level);
              v2 = _eval(v2, ctx, level);
              switch (v2) {
                case 'Hash'   : res = isObj(v1) ; break;
                case 'List'   : res = isArr(v1) ; break;
                case 'String' : res = isStr(v1) ; break;
                case 'Number' : res = isNum(v1) ; break;
                case 'Integer': res = isInt(v1) ; break;
                case 'Boolean': res = isBool(v1); break;
                default       : res = false;
              }
              break
           
            //. .. ... ..  . .. ... .. . .. ... .. . .. ... .. . .. ... .. .
            case '==': res = _eval(v1, ctx, level) == _eval(v2, ctx, level); break;
            case '!=': res = _eval(v1, ctx, level) != _eval(v2, ctx, level); break;
            case '>' : res = _eval(v1, ctx, level) >  _eval(v2, ctx, level); break;
            case '<' : res = _eval(v1, ctx, level) <  _eval(v2, ctx, level); break;
            case '>=': res = _eval(v1, ctx, level) >= _eval(v2, ctx, level); break;
            case '<=': res = _eval(v1, ctx, level) <= _eval(v2, ctx, level); break;
            case '%' : res = _eval(v1, ctx, level) %  _eval(v2, ctx, level); break;
           
            //. .. ... ..  . .. ... .. . .. ... .. . .. ... .. . .. ... .. .
            case true: // Loose checking for truthy|falsey
              v1 = _eval(v1, ctx, level);
              res = !!v1;
              break;
          } // end switch(op)

          // Negate result if flag set true.
          res = _negate ? !res : res;

          // Check next expression's relation.
          // If AND and result is false, break.
          // If OR and result is true, break.
          if (next_expr) {
            if (next_expr.relation === 'and' && !res) {
              break;
            }
            if (next_expr.relation === 'or' && res) {
              break;
            }
          }

          // Check current expression's relation.
          // If not null, combine with previous result.
          if (rel !== null) {
            switch (rel) {
              case 'and': res = (res && prev); break;
              case 'or' : res = (res || prev); break;
            }
          }

          j++;
        } // end while(j < expr_stop)

        // If result has been set stop evaluation
        if (res) {
          break;
        }

        i++;
      } // end while(i < branch_stop)

      if (res)
        out_body = branch.body;
      else if (_has_else)
        out_body = last_branch.body;

      out = '';
      if (out_body && out_body.length > 0) {
        for (i = 0, l = out_body.length; i < l; i++) {
          el = out_body[i];
          out += _eval(el, ctx, level);
        }
      }

      return out;
    }; // end Manana.interpreter.If()

    // ...........................................  
    /**
     * Mañana switch-statement
     * @memberof Manana.interpreter
     * @method Switch
     * @param {Object} form - A Mañana.ast.SwitchNode 
     * @param {*} context - A value to be passed as the context for a view
     * @param {Number} level - The current level used for output formatting
     */
    this.interpreter.Switch = function(form, context, level) {
      var control, i, c, j, len, value, res;

      control = self.interpreter.evalForm(form.control, context, level);

      res = '';

      i = 0; 
      while (c = form.cases[i]) {
        ++i;

        value = self.interpreter.evalForm(c.value, context, level);

        if (value == control) {
          len = c.block.length;
          for (j = 0; j < len; j++) {
            res += self.interpreter.evalForm(c.block[j], context, level);
          }
        }
      }

      if (!res.length && form.else_case) {
        i = 0;
        while (c = form.else_case[i]) { 
          ++i;
          res += self.interpreter.evalForm(c, context, level);
        }
      }

      return res;
    }; // end Manana.interpreter.Switch()

    // ...........................................  
    /**
     * Mañana loop-construct
     * @memberof Manana.interpreter
     * @method For
     * @param {Object} form - A Mañana.ast.ForNode 
     * @param {*} context - A value to be passed as the context for a view
     * @param {Number} level - The current level used for output formatting
     */
    this.interpreter.For = function(form, context, level) {
      var name, loop_name, $parent, scope, local_scope, key, count, total, _is_obj, res;

      function iterate() {
        var i;

        ++count;

        self.namespace[name] = new MananaNamespace(name, scope.data[key], $parent);
        local_scope = self.namespace[name];

        local_scope.$count    = count;
        local_scope.$total    = total;
        local_scope.$is_first = count == 1;
        local_scope.$is_last  = count == total;

        if (_is_obj) {
          local_scope.$key = key;
        } else {
          local_scope.$index    = key;
          local_scope.$previous = scope[key-1] || null;
          local_scope.$next     = scope[key+1] || null;
        }

        self.context = local_scope;  

        if (!isNull(form.body)) {
          i = 0;
          while (!is(form.body[i], "undefined")) {
            if (self._break_loop) {
              cleanUpLoop();
              break;
            }
         
            if (self._continue_loop)
              continue;
         
            res += self.interpreter.evalForm(form.body[i], local_scope, level); 
         
            i++;
          }
        }
      }

      function cleanUpLoop() {
        delete self.namespace[loop_name];
        delete self.namespace[name];
        self._in_loop = false;
        self._break_loop = false;
        self._continue_loop = false;
      }

      $parent = self.context;

      scope = self.interpreter.evalForm(form.path, context, level);
      if (isObj(scope)) {
        _is_obj = true;
      } else if (isArr(scope)) {
        _is_obj = false;
      } else if (isStr(scope)) {
        _is_obj = false;
        scope = scope.split('');
      } else {
        self.err = new MananaError('Invalid context provided to loop. Must be Hash, List, or String.', form.loc);
        throw self.err;
      }

      name = form.id;
      loop_name = '__loop__' + name;

      if (!self.validate.isNamespace(scope))
        scope = new MananaNamespace(loop_name, scope, $parent);
      scope = self.namespace[loop_name] = scope;

      total = _is_obj ? objectSize(scope.data) : scope.data.length;
      count = 0;
      self._in_loop = true;

      res = '';
      if (_is_obj) {
        for (key in scope.data) {
          iterate();
        }
      } else {
        for (key = 0; key < total; key++) {
          iterate();
        }
      }

      cleanUpLoop();

      self.context = $parent;

      return res;
    }; // end Manana.interpreter.For()

    // ...........................................  
    /**
     * Break a loop
     * @memberof Manana.interpreter
     * @method Break
     * @param {Object} form - A Mañana.ast.BreakNode 
     * @param {*} context - A value to be passed as the context for a view
     * @param {Number} level - The current level used for output formatting
     */
    this.interpreter.Break = function(form, context, level) {
      if (!self._in_loop) {
        self.err = new MananaError('Break statement can only exist inside loop!', form.loc);
        throw self.err;
      }
      self._break_loop = true;
      return '';
    }; // end Manana.interpreter.Break()

    // ...........................................  
    /**
     * Continue a loop
     * @memberof Manana.interpreter
     * @method Continue
     * @param {Object} form - A Mañana.ast.ContinueNode 
     * @param {*} context - A value to be passed as the context for a view
     * @param {Number} level - The current level used for output formatting
     */
    this.interpreter.Continue = function(form, context, level) {
      if (!self._in_loop) {
        self.err = new MananaError('Continue statement can only exist inside loop!', form.loc);
        throw self.err;
      }
      self._continue_loop = true;
      return '';
    }; // end Manana.interpreter.Continue()
    
    // ...........................................  
    /**
     * A Mañana string, possibly containing interpolated paths
     * @memberof Manana.interpreter
     * @method String
     * @param {Object} form - A Mañana.ast.StringNode 
     * @param {*} context - A value to be passed as the context for a view
     * @param {Number} level - The current level used for output formatting
     */
    this.interpreter.String = function(form, context, level) {
      var str, ir, res;

      // Remove quotes.
      str = form.body.slice(1, -1);

      // If interpolated, re-parse string as Raw Text. 
      if (str.indexOf('@{') > -1) {
        str = '~' + str + '\n';
        ir  = self.parser.parse(str);
        res = self.interpreter.evalForm(ir[0], context, level);
      }
      // Set res to initial raw string.
      else {
        res = self.interpreter.evalForm(str, context, level);
      }

      return res;
    } // end Manana.interpreter.String()

    // ...........................................  
    /**
     * An HTML tag
     * @memberof Manana.interpreter
     * @method Tag
     * @param {Object} form - A Mañana.ast.TagNode 
     * @param {*} context - A value to be passed as the context for a view
     * @param {Number} level - The current level used for output formatting
     */
    this.interpreter.Tag = function(form, context, level) {
      var html, content, i, key, attr_tpl, attr, next_level, res, node, _indent_next;

      // Create content{} to interpolate in final micro-template later.
      content = {tag: form.tag, attrs: '', body: '', indent: ''};
 
      // Resolve attributes.
      if (form.attrs !== null) {
        attr_tpl = ' {key}="{val}"'; 
        attr = {};

        for (key in form.attrs) {
          attr.key = (key === 'src' && form.tag === 'a') ? "href" : key;
          attr.val = self.interpreter.evalForm(form.attrs[key], context, level);
          content.attrs += strFmt(attr_tpl, attr); 
        }
      }

      // Evaluate nested AST nodes.
      if (isNull(form.body)) {
        _indent_next = false;
      }
      else {
        next_level = level + 1;

        node = form.body;
        _indent_next = (self._format_result && 
                       isArr(node) && 
                       node.length > 0 && 
                       /^Tag|VoidTag|CodeTag$/.test(node[0].type));

        i = 0;
        while (!is(form.body[i], "undefined")) {
          node = form.body[i];
          content.body += self.interpreter.evalForm(node, context, next_level);
          i++;
        }
      }

      // Render final micro-template
      if (_indent_next) {
        html = '{indent}<{tag}{attrs}>\n{body}\n{indent}</{tag}>\n';
        content.indent = repeatStr(self.format.indent_str, level);
      }
      else if (self._format_result) {
        html = '{indent}<{tag}{attrs}>{body}</{tag}>\n';
        content.indent = repeatStr(self.format.indent_str, level);
      }
      else {
        html = '<{tag}{attrs}>{body}</{tag}>';
      }

      res = strFmt(html, content);

      return res;
    }; // end Manana.interpreter.Tag()

    // ...........................................  
    /**
     * An HTML pre or code tag
     * @memberof Manana.interpreter
     * @method CodeTag
     * @param {Object} form - A Mañana.ast.CodeTagNode 
     * @param {*} context - A value to be passed as the context for a view
     * @param {Number} level - The current level used for output formatting
     */
    this.interpreter.CodeTag = function(form, context, level) {
      var html, attr_tpl, attr, content, i, res;

      content = {tag: form.tag, attrs: '', body: ''};

      // Resolve attributes.
      if (form.attrs !== null) {
        attr_tpl = ' {key}="{val}"'; 
        attr = {};

        for (key in form.attrs) {
          attr.key = (key === 'src' && form.tag === 'a') ? "href" : key;
          attr.val = self.interpreter.evalForm(form.attrs[key], context, level);
          content.attrs += strFmt(attr_tpl, attr); 
        }
      }

      content.body = "\n" + form.body.join("\n");

      html = '<{tag}{attrs}>{body}</{tag}>';
      res = strFmt(html, content);

      return res;
    }; // end Manana.interpreter.CodeTag()

    // ...........................................  
    /**
     * An HTML void tag
     * @memberof Manana.interpreter
     * @method VoidTag
     * @param {Object} form - A Mañana.ast.VoidTagNode 
     * @param {*} context - A value to be passed as the context for a view
     * @param {Number} level - The current level used for output formatting
     */
    this.interpreter.VoidTag = function(form, context, level) {
      var html, attr_tpl, attr, content, i, res;

      content = {tag: form.tag, attrs: ''};

      // Resolve attributes.
      if (form.attrs !== null) {
        attr_tpl = ' {key}="{val}"'; 
        attr = {};

        for (key in form.attrs) {
          attr.key = (key === 'src' && form.tag === 'a') ? "href" : key;
          attr.val = self.interpreter.evalForm(form.attrs[key], context, level);
          content.attrs += strFmt(attr_tpl, attr); 
        }
      }

      if (self._format_result) {
        content.indent = repeatStr(self.format.indent_str, level);
        html = '{indent}<{tag}{attrs}>\n';
      } else {
        html = '<{tag}{attrs}>';
      }

      res = strFmt(html, content);

      return res;
    }; // end Manana.interpreter.VoidTag()

    // ...........................................  
    /**
     * Text, possibly containing interpolated paths
     * @memberof Manana.interpreter
     * @method Text
     * @param {Object} form - A Mañana.ast.TextNode 
     * @param {*} context - A value to be passed as the context for a view
     * @param {Number} level - The current level used for output formatting
     */
    this.interpreter.Text = function(form, context, level) {
      var i, res, _eval;

      if (!isNull(form.body)) {
        _eval = self.interpreter.evalForm;

        i = 0; 
        res = '';
        while (!is(form.body[i], "undefined")) {
          res += _eval(form.body[i], context, level);
          i++;
        }
      }

      return res;
    }; // end Manana.interpreter.Text()

    // ...........................................  
    /**
     * Switches parser into "filter mode", allowing block text (by default) or another language injected
     * @memberof Manana.interpreter
     * @method Filter
     * @param {Object} form - A Mañana.ast.FilterNode 
     * @param {*} context - A value to be passed as the context for a view
     * @param {Number} level - The current level used for output formatting
     */
    this.interpreter.Filter = function(form, context, level) {
      var i, res, _eval;

      if (!isNull(form.body)) {
        _eval = self.interpreter.evalForm;

        i = 0; 
        res = '';
        while (!is(form.body[i], "undefined")) {
          res += _eval(form.body[i], context, level);
          i++;
        }
      }

      return res;
    }; // end Manana.interpreter.Filter()

    // _____________________________________________ Validation
    /**
     * Declare if a node is of type MananaNamespace
     * @memberof Manana.validate
     * @method isNamespace
     * @param {*} node - A value to check
     */
    // ...........................................  
    this.validate.isNamespace = function(node) {
      var is_ns = false;

      is_ns = node instanceof MananaNamespace;

      if (!is_ns) {
        if (node
            && node.type 
            && node.type == 'MananaNamespace'
            && node.name
            && !is(node.$parent, 'undefined')
            && !is(node.data, 'undefined'))
        {
          is_ns = true;
        }
      }

      return is_ns;
    }; // end Manana.validate.isNamespace()

    // _____________________________________________ Text Manipulation

    // ...........................................  
    /**
     * Format HTML output from Manana.Render
     * @memberof Manana.text
     * @method encode
     * @param {string} html - The HTML to be formatted
     */
    this.text.encode = function(html) {
      return String(html)
               .replace( /&/g, '&amp;'  )
               .replace( /"/g, '&quot;' )
               .replace( /'/g, '&#39;'  )
               .replace( /</g, '&lt;'   )
               .replace( />/g, '&gt;'   );
    }; // end Manana.text.encode()

    // ...........................................  
    /**
     * Format HTML output from Manana.encode
     * @memberof Manana.text
     * @method decode
     * @param {string} html - The HTML to be formatted
     */
    this.text.decode = function(encoded) {
      return String(encoded)
               .replace( /&amp;/g  , '&' )
               .replace( /&quot;/g , '"' )
               .replace( /&\#39;/g , "'" )
               .replace( /&lt;/g   , '<' )
               .replace( /&gt;/g   , '>' );
    }; // end Manana.text.decode()

    // _____________________________________________ Marshaling

    // ...........................................  
    /**
     * Marshal a View and Context for transportation
     * @memberof Manana.marshal
     * @method bottle
     * @param {string} code - The code to be bottled
     * @param {*} context - A value to be passed as the context for a view
     */
    this.marshal.bottle = function(code, context) {
      var i, lines, line, indent_pat, indent, brew;

      if (is(context, "undefined")) {
        context = {};
      }

      try {
        self.parser.parse(code);
      } catch (e) {
        self.err = new MananaError("Invalid Mañana code sent to Bottle method.");
        throw self.err;
      }

      try {
        JSON.parse(JSON.stringify(context));
      } catch (e) {
        self.err = new MananaError("Invalid context sent to Bottle method.");
        throw self.err;
      }

      lines = code.split(/\n/g);
      indent_pat = /^([\t\s][\t\s]*)/;

      brew = '#ñ(role="template")';
      for (i in lines) {
        line = lines[i];

        indent = line.match(indent_pat);
        if (indent === null) {
          indent = 0;
        } else {
          indent = indent[1].length;
        }

        line = line.replace(indent_pat, '');
        brew += '#ñ{' + indent + '}' + line;
      }

      brew += '#ñ(role="context")' + JSON.stringify(context);

      return brew;
    }; // end Manana.bottle()

    // ...........................................  
    /**
     * UnMarshal a View and Context bottled by Manana.bottle
     * @memberof Manana.marshal
     * @method pour
     * @param {string} brew - The bottle produced by Manana.bottle
     * @param {string} indent_char - The indent string to be used to reformat the source code
     */
    this.marshal.pour = function(brew, indent_char) {
      var parts, tpl, ctx, i, lines, line, indent;

      if (!/^\#ñ\(role="template"\)(?=\#ñ\{)/.test(brew)) {
        self.err = new MananaError("Invalid brew provided to Unbottle method.");
        throw self.err;
      }

      brew = brew.replace('#ñ(role="template")', '');
      parts = brew.split('#ñ(role="context")');
      tpl = parts[0];
      ctx = parts[1];

      try {
        ctx = JSON.parse(ctx);
      } catch (e) {
        self.err = new MananaError("Invalid context provided to Unbottle method.");
        throw self.err;
      }

      if (!indent_char) {
        indent_char = ' ';
      }

      lines = tpl.split(/(\#ñ\{[0-9][0-9]*\})/g);
      if (lines[0] === '') {
        lines = lines.slice(1);
      }

      tpl = '';
      for (i=0, l=lines.length; i < l; i += 2) {
        indent = lines[i]
                   .replace( '#ñ{' , '')
                   .replace( '}'   , '');
        indent = repeatStr(indent_char, parseInt(indent));

        line = lines[i + 1];

        tpl += indent + line + "\n";
      }

      try {
        self.parser.parse(tpl);
      } catch (e) {
        self.err = new MananaError("Invalid Mañana code pourd..");
        throw self.err;
      }

      return { "template": tpl, "context": ctx }
    }; // end Manana.pour()

    // ...........................................  
    /**
     * Call a function inside a view
     * @memberof Manana.interpreter
     * @method Function
     * @param {Object} form - A Mañana.ast.FunctionNode 
     * @param {*} context - A value to be passed as the context for a view
     * @param {Number} level - The current level used for output formatting
     */
    this.interpreter.Function = function(form, context, level) {
      var name, fn, i, args, res, _in_fns, _in_raw_fns;

      name = form.name;

      _in_fns = name in self.fns;
      _in_raw_fns = !_in_fns && name in self.raw_fns;

      if (!_in_fns && !_in_raw_fns) {
        self.err = new MananaError(strFmt('Function "{name}" is not defined.', form.loc));
        throw self.err;
      }

      fn = self.fns[name] || self.raw_fns[name];

      if (!is(fn, "function")) {
        self.err = new MananaError(strFmt('"{name}" is not a function', form.loc));
        throw self.err;
      }

      if (_in_raw_fns) {
        args = form.args;
      } else {
        args = [];
        if (form.args) {
          i = 0;
          while (!is(form.args[i], "undefined")) {
            args.push(self.interpreter.evalForm(form.args[i], context, level));
            i++;
          }
        }
      }

      try {
        res = fn.apply(self, args);
      } catch (e) {
        self.err = new MananaError(e.message, form.loc);
        throw self.err;
      }

      return res;
    }; // end Manana.interpreter.Function()

    // ...........................................  
    this.addFunction = function(name, fn) {
      if (!isStr(name)) {
        self.err = new MananaError("1st arg to Manana.add_fn() must be a string");
        throw self.err
      }

      if (name in self.raw_fns) {
        self.err = new MananaError('Function "' + name + '" already exists!');
        throw self.err
      }

      if (!is(fn, "function")) {
        self.err = new MananaError("2nd arg to Manana.add_fn() must be a function");
        throw self.err
      }

      self.fns[name] = fn;
    }; // end Manana.addFunction()

    // ...........................................  
    this.addRawFunction = function(name, fn) {
      if (!isStr(name)) {
        self.err = new MananaError("1st arg to Manana.add_raw_fn() must be a string");
        throw self.err
      }

      if (name in self.fns) {
        self.err = new MananaError('Function "' + name + '" already exists!');
        throw self.err
      }

      if (!is(fn, "function")) {
        self.err = new MananaError("2nd arg to Manana.add_raw_fn() must be a function");
        throw self.err
      }

      self.raw_fns[name] = fn;
    }; // end Manana.addRawFunction()

    // ...........................................  
    // switching to camelcase, leaving for backward compatability
    this.add_fn = this.addFunction; 
    this.add_raw_fn = this.addRawFunction; 



    // _____________________________________________ Built-in functions

    // ...........................................  
    /**
     * Get the length (size) of a Hash, List, String, or Number
     * @memberof Manana.fns
     * @method len
     * @param {Hash|List|String|Number} value - A value that has a lenght/size
     */
    self.fns.len = function(value) {
      if (isObj(value))
        return objectSize(value);
      else if (isArr(value) || isStr(value))
        return value.length;
      else if (isNum(value))
        return value.toString().length;

      self.err = new MananaError('Invalid value in Len function. Must be Hash, List, String, or Number');
      throw self.err;
    }; // end Manana.len()

    // ...........................................  
    /**
     * Log current View, Context, and the Manana interpreter to console
     * @memberof Manana.fns
     * @method debug
     * @param {Object} form - A Mañana AST node 
     */
    self.fns.debug = function(form) {
      console.log('view: ', self.view);
      console.log('context: ', self.context);
      console.log('Manana: ', self);
      return '';
    }; // end Manana.debug()

    // ...........................................  
    /**
     * Print one or more values in the current view
     * @memberof Manana.fns
     * @method print
     * @param {*} arguments... - One or more arguments to print
     */
    self.fns.print = function() {
      var res = '', i = 0;
      while (!is(arguments[i], "undefined")) {
        res += JSON.stringify(arguments[i], null, 4);
        i++;
      }
      return res;
    }; // end Manana.print()

    // ...........................................  
    /**
     * Log a value to the console
     * @memberof Manana.fns
     * @method log
     * @param {*} value - Any value
     */
    self.fns.log = function(value) {
      console.log(value);
      return '';
    }; // end Manana.log()

    // ...........................................  
    /**
     * Print the current context to the view
     * @memberof Manana.fns
     * @method context
     */
    self.fns.context = function(_pretty) {
      return '<pre>\n' + JSON.stringify(self.context, null, 4) + '</pre>';
    }; // end Manana.context()

    // ...........................................  
    /**
     * Print the current view inside the view 
     * @memberof Manana.fns
     * @method view
     */
    self.fns.view = function(form) {
      var out = JSON.stringify(self.view, null, 4).split("\n")
      out.unshift("<pre>");
      out.push("</pre>");
      return out.join("\n    ");
    }; // end Manana.view()

    // ...........................................  
    /**
     * Print value to view and log it to console
     * @memberof Manana.fns
     * @method whatis
     * @param {*} value - Any value that should be printed and logged.
     */
    self.fns.whatis = function(value) {
      console.log('@whatis: ', value);
      return '<pre>' + JSON.stringify(value, null, 4) + '</pre>';
    };

    // _____________________________________________ Built-in raw functions
    
    // ........................................... 
    /**
     * Determine the first argument that does not throw an error
     * @memberof Manana.fns
     * @method first_valid
     * @param {*} arguments... - n-number of arguments to evaluate
     */
    self.raw_fns.first_valid = function() {
      var i, arg, res; 

      silence();

      i = 0;
      while (!is(arg = arguments[i], 'undefined')) {
        ++i;
        try {
          res = self.interpreter.evalForm(arg, self.context);
          break;
        } 
        catch (e) {
          continue;
        } 
        finally {
          unsilence();
        }
      }

      if (!is(res, 'undefined'))
        return res;
      
      self.err = new MananaError("No valid argument in First Valid function.");
      throw self.err
    }; // end Manana.first_valid()

    // _____________________________________________ Exports
    // export related classes
    exports['MananaNamespace'] = MananaNamespace;
    exports['MananaView'] = MananaView;
    exports['MananaError'] = MananaError;
  } // end Manana() 



  // _____________________________________________ Make available in both node.js & browser 
  if (_manana_is_server_side) {
    exports['Manana'] = Manana;
  }

  if (typeof window === 'undefined') {
    window = {};
  }
  window['Manana'] = Manana;
  window['MananaNamespace'] = exports['MananaNamespace'];
  window['MananaView'] = exports['MananaView'];
  window['MananaError'] = exports['MananaError'];

})(typeof exports === "undefined" ? {} : exports);
